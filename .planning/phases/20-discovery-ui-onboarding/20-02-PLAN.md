---
phase: 20-discovery-ui-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/venus_os_fronius_proxy/static/index.html
  - src/venus_os_fronius_proxy/static/style.css
  - src/venus_os_fronius_proxy/static/app.js
autonomous: false
requirements: [DISC-05, CONF-04, UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "User sees a discover button (magnifying glass icon) in inverter panel header, left of + button"
    - "Clicking discover button triggers POST /api/scanner/discover and shows progress bar below inverter panel"
    - "Progress bar fills with blue and shows phase text: Scanning ... then Verifying SunSpec ..."
    - "Discover button is disabled with spinner icon during scan"
    - "Scan results appear as checkbox list with manufacturer, model, host:port, unit ID"
    - "Already-configured inverters are greyed out with Bereits konfiguriert label"
    - "Add All button above results adds all checked devices via POST /api/inverters"
    - "Empty scan shows orange hint card with troubleshooting tips"
    - "When config page opens with empty inverter list, scan starts automatically"
    - "Single auto-scan result is auto-added with toast notification"
    - "Scan-Ports field below inverter list shows comma-separated ports, persisted via PUT /api/scanner/config"
  artifacts:
    - path: "src/venus_os_fronius_proxy/static/index.html"
      provides: "Discover button, scan area, ports field HTML"
      contains: "btn-discover-inverter"
    - path: "src/venus_os_fronius_proxy/static/style.css"
      provides: "Scan progress bar, result row, ports field styles"
      contains: "ve-scan-bar"
    - path: "src/venus_os_fronius_proxy/static/app.js"
      provides: "WS message handlers, scan UI logic, auto-scan, batch add"
      contains: "handleScanProgress"
  key_links:
    - from: "app.js btn-discover-inverter click"
      to: "POST /api/scanner/discover"
      via: "fetch in triggerScan()"
      pattern: "fetch.*api/scanner/discover"
    - from: "app.js ws.onmessage"
      to: "handleScanProgress, handleScanComplete, handleScanError"
      via: "msg.type routing"
      pattern: "msg\\.type === 'scan_progress'"
    - from: "app.js addDiscoveredInverters"
      to: "POST /api/inverters"
      via: "sequential fetch calls"
      pattern: "fetch.*api/inverters.*POST"
    - from: "app.js loadInverters"
      to: "triggerScan"
      via: "empty list auto-scan check"
      pattern: "_autoScanDone.*triggerScan"
---

<objective>
Build the complete discovery UI: discover button with icon, progress bar, scan result list with checkboxes, auto-scan onboarding, and scan ports configuration field.

Purpose: Users can discover inverters on their network with one click, see real-time scan progress, and add found inverters to their config. New installations get zero-config onboarding.
Output: Complete discovery UI in index.html, style.css, and app.js.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-discovery-ui-onboarding/20-CONTEXT.md
@.planning/phases/20-discovery-ui-onboarding/20-RESEARCH.md
@.planning/phases/20-discovery-ui-onboarding/20-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- backend endpoints the frontend will call -->

POST /api/scanner/discover
  Request: {"ports": [502, 1502]} (optional, uses config default)
  Response: {"status": "started"} (200) or {"error": "Scan already running"} (409)
  Side effect: Starts background scan, streams progress via WS

GET /api/scanner/config
  Response: {"ports": [502, 1502]}

PUT /api/scanner/config
  Request: {"ports": [502, 1502, 8502]}
  Response: {"success": true}

WS message types (received by frontend):
  {"type": "scan_progress", "data": {"phase": "probe"|"verify", "current": int, "total": int}}
  {"type": "scan_complete", "data": {"devices": [...], "count": int}}
  {"type": "scan_error", "data": {"error": "string"}}

Device object in scan_complete:
  {ip, port, unit_id, manufacturer, model, serial_number, firmware_version, supported}

POST /api/inverters (existing)
  Request: {host, port, unit_id, manufacturer, model, serial, firmware_version, enabled: true}
  Response: 201 with entry dict

GET /api/inverters (existing)
  Response: {"inverters": [{host, port, unit_id, id, enabled, active, manufacturer, model, serial, firmware_version}, ...]}

<!-- Existing frontend patterns from app.js -->
loadInverters(invertersData?) -- fetches and renders inverter list
showToast(message, type) -- 'success', 'error', 'warning', 'info'
createInverterRow(inv) -- creates a single inverter row element
ws.onmessage routes by msg.type: 'snapshot', 'history', 'override_event'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML structure + CSS styles for discovery UI elements</name>
  <read_first>
    - src/venus_os_fronius_proxy/static/index.html (lines 260-286 for inverter panel structure)
    - src/venus_os_fronius_proxy/static/style.css (search for ve-inv, ve-panel-header, ve-hint-card, ve-btn for patterns)
    - CLAUDE.md (design system tokens, naming conventions)
  </read_first>
  <files>src/venus_os_fronius_proxy/static/index.html, src/venus_os_fronius_proxy/static/style.css</files>
  <action>
**HTML changes in index.html** (inverter panel, lines ~262-286):

1. Add discover button in `.ve-panel-header`, LEFT of the existing `#btn-add-inverter`:
```html
<div class="ve-panel-header">
  <h2>Inverters</h2>
  <button type="button" class="ve-btn ve-btn--sm ve-btn--primary" id="btn-discover-inverter" title="Auto-Discover">
    <svg class="ve-btn-icon" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
      <circle cx="7" cy="7" r="4.5" stroke="currentColor" stroke-width="1.5"/>
      <line x1="10.5" y1="10.5" x2="14" y2="14" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    </svg>
  </button>
  <button type="button" class="ve-btn ve-btn--sm ve-btn--primary" id="btn-add-inverter" title="Add Inverter">+</button>
</div>
```

2. Add scan ports field AFTER `#inverter-add-form` (before closing `</div>` of `#inverter-panel`):
```html
<div id="scan-ports-field" class="ve-scan-ports">
  <label class="ve-scan-ports-label">Scan-Ports:</label>
  <input type="text" class="ve-input ve-scan-ports-input" id="scan-ports-input" value="502, 1502" placeholder="502, 1502">
</div>
```

3. Add scan area AFTER `#scan-ports-field` (still inside `#inverter-panel`):
```html
<div id="scan-area" style="display:none">
  <div id="scan-progress" class="ve-scan-progress">
    <div class="ve-scan-bar">
      <div class="ve-scan-bar-fill" id="scan-bar-fill"></div>
    </div>
    <span class="ve-scan-status" id="scan-status-text"></span>
  </div>
  <div id="scan-results"></div>
</div>
```

**CSS additions in style.css:**

```css
/* === Discovery / Scan === */
.ve-btn-icon {
  width: 14px;
  height: 14px;
  vertical-align: middle;
}

.ve-scan-ports {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  border-top: 1px solid var(--ve-border);
}
.ve-scan-ports-label {
  font-size: 0.85rem;
  color: var(--ve-text-dim);
  white-space: nowrap;
}
.ve-scan-ports-input {
  max-width: 160px;
  font-size: 0.85rem;
}

.ve-scan-progress {
  padding: 12px 16px;
}
.ve-scan-bar {
  height: 6px;
  background: var(--ve-border);
  border-radius: 3px;
  overflow: hidden;
}
.ve-scan-bar-fill {
  height: 100%;
  width: 0%;
  background: var(--ve-blue);
  border-radius: 3px;
  transition: width var(--ve-duration-fast) var(--ve-easing-default);
}
.ve-scan-status {
  display: block;
  margin-top: 6px;
  font-size: 0.85rem;
  color: var(--ve-text-dim);
}

.ve-scan-results-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8px 16px 4px;
}
.ve-scan-results-header span {
  font-size: 0.85rem;
  color: var(--ve-text-dim);
}

.ve-scan-result {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 16px;
  border-top: 1px solid var(--ve-border);
  font-size: 0.85rem;
}
.ve-scan-result--configured {
  opacity: 0.5;
}
.ve-scan-result-check {
  flex-shrink: 0;
}
.ve-scan-result-check input[type="checkbox"] {
  width: 16px;
  height: 16px;
  accent-color: var(--ve-blue);
  cursor: pointer;
}
.ve-scan-result-configured {
  font-size: 0.8rem;
  color: var(--ve-text-dim);
  font-style: italic;
  min-width: 120px;
}
.ve-scan-result-identity {
  flex: 1;
  color: var(--ve-text);
}
.ve-scan-result-host {
  font-family: var(--ve-mono);
  color: var(--ve-text-secondary);
  min-width: 130px;
}
.ve-scan-result-unit {
  color: var(--ve-text-dim);
  font-size: 0.8rem;
  min-width: 50px;
}

.ve-scan-hint {
  margin: 8px 16px 12px;
}

/* Discover button spinner state */
#btn-discover-inverter.ve-scanning {
  pointer-events: none;
  opacity: 0.7;
}
#btn-discover-inverter.ve-scanning .ve-btn-icon {
  animation: ve-spin 1s linear infinite;
}
@keyframes ve-spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* Responsive: scan results */
@media (max-width: 480px) {
  .ve-scan-result {
    flex-wrap: wrap;
    gap: 4px;
  }
  .ve-scan-result-host {
    min-width: auto;
    width: 100%;
  }
  .ve-scan-ports {
    flex-wrap: wrap;
  }
}
```
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && grep -c "btn-discover-inverter" src/venus_os_fronius_proxy/static/index.html && grep -c "ve-scan-bar-fill" src/venus_os_fronius_proxy/static/style.css && grep -c "scan-ports-input" src/venus_os_fronius_proxy/static/index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="btn-discover-inverter"` with SVG magnifying glass icon
    - index.html `#btn-discover-inverter` appears BEFORE `#btn-add-inverter` in the panel header
    - index.html contains `id="scan-ports-field"` with label "Scan-Ports:" and input `id="scan-ports-input"`
    - index.html contains `id="scan-area"` with `style="display:none"`
    - index.html contains `id="scan-bar-fill"` inside `.ve-scan-bar`
    - index.html contains `id="scan-results"` div
    - style.css contains `.ve-scan-bar` with `height: 6px` and `background: var(--ve-border)`
    - style.css contains `.ve-scan-bar-fill` with `background: var(--ve-blue)`
    - style.css contains `.ve-scan-result` with flex layout
    - style.css contains `.ve-scan-result--configured` with `opacity: 0.5`
    - style.css contains `.ve-scan-ports` with flex layout
    - style.css contains `@keyframes ve-spin` for spinner animation
    - No hardcoded hex colors (all use `var(--ve-*)`)
  </acceptance_criteria>
  <done>All HTML elements for discovery UI are in place, all CSS styles use design system tokens, discover button has magnifying glass SVG, scan area hidden by default</done>
</task>

<task type="auto">
  <name>Task 2: Discovery JS logic -- WS handlers, scan trigger, results, auto-scan, ports</name>
  <read_first>
    - src/venus_os_fronius_proxy/static/app.js (full file -- need WS routing at ~line 96, loadInverters at ~line 816, showToast at ~line 1433, inverter CRUD patterns)
    - src/venus_os_fronius_proxy/static/index.html (to see the HTML elements created in Task 1)
    - .planning/phases/20-discovery-ui-onboarding/20-CONTEXT.md (locked decisions for auto-scan behavior)
  </read_first>
  <files>src/venus_os_fronius_proxy/static/app.js</files>
  <action>
Add all discovery JS logic to app.js. Use vanilla JS (no ES6 modules, no arrow functions in event handlers -- match existing codebase style with `var` and `function` expressions).

**1. Module-level state variables** (add near top of file, after existing globals):
```javascript
var _scanRunning = false;
var _autoScanDone = false;
var _configuredInverters = [];  // cached for duplicate detection
```

**2. WS message routing** -- in `ws.onmessage` handler (around line 99-101), add after existing type checks:
```javascript
if (msg.type === 'scan_progress') handleScanProgress(msg.data);
if (msg.type === 'scan_complete') handleScanComplete(msg.data);
if (msg.type === 'scan_error') handleScanError(msg.data);
```

**3. Scan progress handler:**
```javascript
function handleScanProgress(data) {
    var area = document.getElementById('scan-area');
    area.style.display = '';
    var fill = document.getElementById('scan-bar-fill');
    var statusText = document.getElementById('scan-status-text');
    var progress = document.getElementById('scan-progress');
    progress.style.display = '';

    var pct = data.total > 0 ? Math.round((data.current / data.total) * 100) : 0;
    fill.style.width = pct + '%';

    if (data.phase === 'probe') {
        statusText.textContent = 'Scanning network (' + data.current + '/' + data.total + ')...';
    } else if (data.phase === 'verify') {
        statusText.textContent = 'Verifying SunSpec (' + data.current + '/' + data.total + ')...';
    }
}
```

**4. Scan complete handler:**
```javascript
function handleScanComplete(data) {
    _scanRunning = false;
    setScanButtonState(false);

    var progress = document.getElementById('scan-progress');
    progress.style.display = 'none';
    var resultsDiv = document.getElementById('scan-results');
    resultsDiv.innerHTML = '';

    var devices = data.devices || [];

    if (devices.length === 0) {
        // Show hint card with tips
        resultsDiv.innerHTML =
            '<div class="ve-hint-card ve-scan-hint">' +
            '<div class="ve-hint-header">Keine Inverter gefunden</div>' +
            '<div class="ve-hint-subtext">Pruefe: Sind die Geraete eingeschaltet? Ist Modbus TCP aktiviert? Stimmen die Ports?</div>' +
            '</div>';
        return;
    }

    // Auto-scan single result: auto-add
    if (_autoScanDone && devices.length === 1 && !isAlreadyConfigured(devices[0])) {
        addDiscoveredInverters([devices[0]]);
        return;
    }

    // Build result list
    var header = document.createElement('div');
    header.className = 've-scan-results-header';
    header.innerHTML = '<span>' + devices.length + ' Inverter gefunden</span>' +
        '<button type="button" class="ve-btn ve-btn--sm ve-btn--save" id="btn-scan-add-all">Alle uebernehmen</button>';
    resultsDiv.appendChild(header);

    var hasNewDevices = false;
    devices.forEach(function(device) {
        var configured = isAlreadyConfigured(device);
        if (!configured) hasNewDevices = true;
        resultsDiv.appendChild(createScanResultRow(device, configured));
    });

    // Wire Add All button
    document.getElementById('btn-scan-add-all').addEventListener('click', function() {
        var checked = [];
        resultsDiv.querySelectorAll('.ve-scan-result').forEach(function(row) {
            if (row._device && !row.classList.contains('ve-scan-result--configured')) {
                var cb = row.querySelector('input[type="checkbox"]');
                if (cb && cb.checked) checked.push(row._device);
            }
        });
        if (checked.length > 0) addDiscoveredInverters(checked);
    });

    if (!hasNewDevices) {
        header.querySelector('#btn-scan-add-all').style.display = 'none';
    }
}
```

**5. Scan error handler:**
```javascript
function handleScanError(data) {
    _scanRunning = false;
    setScanButtonState(false);
    var progress = document.getElementById('scan-progress');
    progress.style.display = 'none';
    showToast('Scan fehlgeschlagen: ' + (data.error || 'Unbekannter Fehler'), 'error');
}
```

**6. Helper: create scan result row:**
```javascript
function createScanResultRow(device, isConfigured) {
    var row = document.createElement('div');
    row.className = 've-scan-result';
    if (isConfigured) row.classList.add('ve-scan-result--configured');

    var identity = ((device.manufacturer || '') + ' ' + (device.model || '')).trim() || 'Unknown';
    var hostPort = device.ip + ':' + device.port;

    if (isConfigured) {
        row.innerHTML =
            '<span class="ve-scan-result-configured">Bereits konfiguriert</span>' +
            '<span class="ve-scan-result-host">' + hostPort + '</span>' +
            '<span class="ve-scan-result-identity">' + identity + '</span>' +
            '<span class="ve-scan-result-unit">Unit ' + device.unit_id + '</span>';
    } else {
        row.innerHTML =
            '<label class="ve-scan-result-check"><input type="checkbox" checked></label>' +
            '<span class="ve-scan-result-host">' + hostPort + '</span>' +
            '<span class="ve-scan-result-identity">' + identity + '</span>' +
            '<span class="ve-scan-result-unit">Unit ' + device.unit_id + '</span>';
    }
    row._device = device;
    return row;
}
```

**7. Helper: duplicate detection:**
```javascript
function isAlreadyConfigured(device) {
    return _configuredInverters.some(function(inv) {
        return inv.host === device.ip &&
               inv.port === device.port &&
               inv.unit_id === device.unit_id;
    });
}
```

**8. Helper: batch add discovered inverters:**
```javascript
async function addDiscoveredInverters(devices) {
    var added = 0;
    for (var i = 0; i < devices.length; i++) {
        var d = devices[i];
        try {
            var res = await fetch('/api/inverters', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    host: d.ip, port: d.port, unit_id: d.unit_id,
                    manufacturer: d.manufacturer || '', model: d.model || '',
                    serial: d.serial_number || '', firmware_version: d.firmware_version || '',
                    enabled: true
                })
            });
            if (res.status === 201) added++;
        } catch (e) {
            console.error('Failed to add inverter:', d.ip, e);
        }
    }
    if (added > 0) {
        showToast(added === 1 ? 'Inverter hinzugefuegt' : added + ' Inverter hinzugefuegt', 'success');
        // Clear scan results and reload list
        document.getElementById('scan-results').innerHTML = '';
        document.getElementById('scan-area').style.display = 'none';
        loadInverters();
    }
}
```

**9. Trigger scan function:**
```javascript
async function triggerScan() {
    if (_scanRunning) return;
    _scanRunning = true;
    setScanButtonState(true);

    // Clear previous results
    document.getElementById('scan-results').innerHTML = '';
    document.getElementById('scan-bar-fill').style.width = '0%';
    document.getElementById('scan-area').style.display = '';
    document.getElementById('scan-progress').style.display = '';
    document.getElementById('scan-status-text').textContent = 'Starting scan...';

    // Read ports from input
    var portsInput = document.getElementById('scan-ports-input');
    var portsStr = portsInput ? portsInput.value : '502, 1502';
    var ports = portsStr.split(',').map(function(p) { return parseInt(p.trim(), 10); }).filter(function(p) { return !isNaN(p) && p > 0 && p <= 65535; });
    if (ports.length === 0) ports = [502, 1502];

    try {
        var res = await fetch('/api/scanner/discover', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ ports: ports })
        });
        if (res.status === 409) {
            showToast('Scan laeuft bereits', 'warning');
            _scanRunning = false;
            setScanButtonState(false);
        }
    } catch (e) {
        showToast('Scan konnte nicht gestartet werden: ' + e.message, 'error');
        _scanRunning = false;
        setScanButtonState(false);
    }
}
```

**10. Button state helper:**
```javascript
function setScanButtonState(scanning) {
    var btn = document.getElementById('btn-discover-inverter');
    if (!btn) return;
    if (scanning) {
        btn.classList.add('ve-scanning');
        btn.disabled = true;
        btn.title = 'Scan laeuft...';
    } else {
        btn.classList.remove('ve-scanning');
        btn.disabled = false;
        btn.title = 'Auto-Discover';
    }
}
```

**11. Modify `loadInverters` function** to cache configured inverters and trigger auto-scan:

At the END of `loadInverters`, after rendering inverter rows, add:
```javascript
// Cache for duplicate detection
_configuredInverters = inverters;
```

At the BEGINNING of `loadInverters`, when inverters list is empty (the block that currently may show an empty state), add auto-scan trigger:
```javascript
if (!inverters || inverters.length === 0) {
    _configuredInverters = [];
    // Auto-scan: only on page navigation, not after mutations
    if (!_scanRunning && !_autoScanDone) {
        _autoScanDone = true;
        triggerScan();
    }
    return;
}
```

**12. Reset `_autoScanDone` on page navigation** -- find the page navigation handler (where hash/data-page is read), add when navigating TO config:
```javascript
// When navigating to config page, reset auto-scan flag
if (page === 'config') _autoScanDone = false;
```

**13. Wire discover button click** (in DOMContentLoaded or init section):
```javascript
document.getElementById('btn-discover-inverter').addEventListener('click', function() {
    _autoScanDone = false;  // Manual scan, allow auto-scan logic
    triggerScan();
});
```

**14. Load and save scan ports** -- on config load (in the config fetch handler where venus config is loaded), also fetch scanner config:
```javascript
// Load scanner ports
fetch('/api/scanner/config').then(function(r) { return r.json(); }).then(function(data) {
    var input = document.getElementById('scan-ports-input');
    if (input && data.ports) input.value = data.ports.join(', ');
});
```

Save ports on blur of the input field:
```javascript
document.getElementById('scan-ports-input').addEventListener('blur', function() {
    var ports = this.value.split(',').map(function(p) { return parseInt(p.trim(), 10); }).filter(function(p) { return !isNaN(p) && p > 0 && p <= 65535; });
    if (ports.length === 0) return;
    fetch('/api/scanner/config', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ ports: ports })
    });
});
```
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && grep -c "handleScanProgress" src/venus_os_fronius_proxy/static/app.js && grep -c "handleScanComplete" src/venus_os_fronius_proxy/static/app.js && grep -c "triggerScan" src/venus_os_fronius_proxy/static/app.js && grep -c "addDiscoveredInverters" src/venus_os_fronius_proxy/static/app.js && grep -c "_autoScanDone" src/venus_os_fronius_proxy/static/app.js && grep -c "isAlreadyConfigured" src/venus_os_fronius_proxy/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - app.js contains `function handleScanProgress(data)` with phase text "Scanning network" and "Verifying SunSpec"
    - app.js contains `function handleScanComplete(data)` that shows checkbox list or hint card
    - app.js contains `function handleScanError(data)` that re-enables button
    - app.js ws.onmessage contains `msg.type === 'scan_progress'`
    - app.js ws.onmessage contains `msg.type === 'scan_complete'`
    - app.js ws.onmessage contains `msg.type === 'scan_error'`
    - app.js contains `function triggerScan()` that POSTs to `/api/scanner/discover`
    - app.js contains `function addDiscoveredInverters(devices)` that POSTs to `/api/inverters`
    - app.js contains `function isAlreadyConfigured(device)` comparing host+port+unit_id
    - app.js contains `function createScanResultRow(device, isConfigured)` with "Bereits konfiguriert"
    - app.js contains `function setScanButtonState(scanning)` toggling `ve-scanning` class
    - app.js contains `_autoScanDone` variable and auto-scan logic in loadInverters
    - app.js contains `btn-discover-inverter` click handler calling triggerScan
    - app.js contains fetch to `/api/scanner/config` for loading ports
    - app.js contains PUT `/api/scanner/config` on scan-ports-input blur
    - app.js handleScanComplete contains "Keine Inverter gefunden" hint card for empty results
    - app.js handleScanComplete contains "Alle uebernehmen" button for Add All
    - app.js `_autoScanDone = false` is set when navigating to config page
  </acceptance_criteria>
  <done>All discovery JS functions implemented, WS handlers route scan messages, discover button triggers scan, results show as checkbox list with duplicate detection, auto-scan fires on empty inverter list, ports field loads and saves via API</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify discovery UI end-to-end</name>
  <files>src/venus_os_fronius_proxy/static/index.html, src/venus_os_fronius_proxy/static/style.css, src/venus_os_fronius_proxy/static/app.js</files>
  <action>
Human verification of the complete discovery UI. What was built:
- Discover button with magnifying glass icon in inverter panel header, left of + button
- Progress bar with phase text below inverter panel during scan
- Scan result list with checkboxes for each discovered inverter
- Auto-scan on empty inverter list when opening config page
- Ports field with persistence via API
- Add All button for batch adding
- Duplicate detection (greyed out with "Bereits konfiguriert")
- Empty scan hint card with troubleshooting tips

Steps to verify:
1. Open config page in browser (http://localhost:80/#config or equivalent)
2. Verify discover button (magnifying glass icon) appears left of the + button in Inverters panel header
3. Verify "Scan-Ports:" field appears below inverter list with "502, 1502" default
4. Click discover button -- verify:
   a. Button gets disabled with spinning icon
   b. Progress bar appears below inverter panel with blue fill
   c. Status text shows "Scanning network (X/Y)..." then "Verifying SunSpec (X/Y)..."
   d. Venus OS config remains usable during scan
5. After scan completes -- verify:
   a. Results appear as checkbox list with manufacturer, model, host:port, unit ID
   b. Already-configured inverters show greyed out with "Bereits konfiguriert"
   c. "Alle uebernehmen" button appears above results
6. Check an inverter and click "Alle uebernehmen" -- verify it gets added to inverter list
7. Delete all inverters, navigate away and back to config -- verify auto-scan starts
8. Change ports to "502" and blur the field, refresh page -- verify "502" persists
  </action>
  <verify>Human visual and functional verification of the discovery UI flow</verify>
  <acceptance_criteria>
    - Discover button visible in panel header with magnifying glass SVG icon
    - Progress bar fills with blue during scan
    - Scan results appear as checkbox list
    - Already-configured inverters greyed out
    - Auto-scan triggers on empty inverter list
    - Ports field persists across refresh
  </acceptance_criteria>
  <done>User approves the discovery UI end-to-end flow</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All scan UI elements render correctly in the browser
- Progress bar fills during scan with correct phase text
- Scan results appear as checkbox list with proper styling
- Already-configured inverters are greyed out
- Auto-scan triggers on empty inverter list
- Ports field persists across page refresh
- Manual discover button works, stays disabled during scan
- Error states handled (scan fail shows toast)
- Full test suite passes: `pytest tests/ -x`
</verification>

<success_criteria>
- User can trigger network scan via discover button and see real-time progress
- Scan results appear as selectable list, confirming adds them to config
- Auto-scan runs when config page opens with no inverters configured
- Ports field persists in config YAML via API
- All design system tokens used (no hardcoded colors)
- Responsive layout works on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/20-discovery-ui-onboarding/20-02-SUMMARY.md`
</output>
