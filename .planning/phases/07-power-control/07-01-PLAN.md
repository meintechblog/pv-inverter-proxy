---
phase: 07-power-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/venus_os_fronius_proxy/control.py
  - src/venus_os_fronius_proxy/webapp.py
  - src/venus_os_fronius_proxy/proxy.py
  - src/venus_os_fronius_proxy/dashboard.py
  - tests/test_control.py
  - tests/test_webapp.py
  - tests/test_proxy.py
autonomous: true
requirements: [CTRL-04, CTRL-05, CTRL-06, CTRL-07, CTRL-08, CTRL-09, CTRL-10]

must_haves:
  truths:
    - "ControlState tracks last_source (none/venus_os/webapp) and last_change_ts"
    - "OverrideLog stores last 50 control events as dicts with ts/source/action/value/detail"
    - "POST /api/power-limit sets limit with validation and returns success/error from WriteResult"
    - "POST /api/power-limit returns 409 when Venus OS wrote within last 60s"
    - "EDPC refresh loop re-writes limit every 30s and handles auto-revert after 5min timeout"
    - "Venus OS writes via _handle_control_write set last_source=venus_os and cancel webapp revert"
    - "Dashboard snapshot includes last_source, last_change_ts, revert_remaining_s, and override_log"
  artifacts:
    - path: "src/venus_os_fronius_proxy/control.py"
      provides: "Extended ControlState with source tracking, OverrideLog, EdpcRefreshLoop"
      contains: "class OverrideLog"
    - path: "src/venus_os_fronius_proxy/webapp.py"
      provides: "POST /api/power-limit endpoint"
      contains: "power_limit_handler"
    - path: "tests/test_control.py"
      provides: "Tests for ControlState extensions, OverrideLog, EDPC refresh"
      contains: "test_edpc_refresh"
    - path: "tests/test_webapp.py"
      provides: "Tests for POST /api/power-limit"
      contains: "test_power_limit"
  key_links:
    - from: "src/venus_os_fronius_proxy/webapp.py"
      to: "src/venus_os_fronius_proxy/control.py"
      via: "power_limit_handler reads/writes ControlState and OverrideLog"
      pattern: "control_state\\.set_from_webapp|override_log\\.append"
    - from: "src/venus_os_fronius_proxy/proxy.py"
      to: "src/venus_os_fronius_proxy/control.py"
      via: "_handle_control_write sets last_source=venus_os"
      pattern: "last_source.*=.*venus_os"
    - from: "src/venus_os_fronius_proxy/dashboard.py"
      to: "src/venus_os_fronius_proxy/control.py"
      via: "collect() includes extended control state in snapshot"
      pattern: "last_source|last_change_ts|revert_remaining"
---

<objective>
Backend power control: extend ControlState with source tracking and auto-revert, add OverrideLog ring buffer, implement EDPC refresh loop, add POST /api/power-limit endpoint, update Venus OS override tracking in proxy.py, and extend dashboard snapshot.

Purpose: All backend primitives for power control with safety (Venus OS priority, auto-revert, source tracking).
Output: Extended control.py, new REST endpoint in webapp.py, updated proxy.py and dashboard.py, comprehensive tests.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-power-control/07-CONTEXT.md
@.planning/phases/07-power-control/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/venus_os_fronius_proxy/control.py:
```python
class ControlState:
    wmaxlim_ena: int = 0
    wmaxlimpct_raw: int = 0
    scale_factor: int = WMAXLIMPCT_SF
    # Properties:
    is_enabled -> bool
    wmaxlimpct_float -> float
    # Methods:
    update_wmaxlimpct(raw_value: int) -> None
    update_wmaxlim_ena(value: int) -> None
    get_model_123_readback() -> list[int]
    is_model_123_address(address: int, count: int) -> bool

def validate_wmaxlimpct(raw_value: int, scale_factor: int = WMAXLIMPCT_SF) -> str | None
def wmaxlimpct_to_se_registers(raw_value: int, scale_factor: int = WMAXLIMPCT_SF) -> tuple[int, int]
```

From src/venus_os_fronius_proxy/webapp.py:
```python
async def create_webapp(shared_ctx: dict, config: Config, config_path: str, plugin: object) -> web.AppRunner
async def broadcast_to_clients(app: web.Application, snapshot: dict) -> None
# shared_ctx keys: "cache", "conn_mgr", "control_state", "poll_counter", "dashboard_collector"
# app keys: "shared_ctx", "config", "config_path", "plugin", "ws_clients"
```

From src/venus_os_fronius_proxy/proxy.py:
```python
class StalenessAwareSlaveContext:
    _control: ControlState | None
    _plugin: InverterPlugin | None
    _shared_ctx: dict | None
    async def _handle_control_write(abs_addr: int, values: list[int]) -> None
```

From src/venus_os_fronius_proxy/dashboard.py:
```python
class DashboardCollector:
    def collect(self, cache, control_state=None, conn_mgr=None, poll_counter=None) -> dict
    # Returns: {"ts": float, "inverter": {...}, "control": {"enabled", "limit_pct", "wmaxlimpct_raw"}, "connection": {...}}
```

From src/venus_os_fronius_proxy/plugins/solaredge.py:
```python
# WriteResult = namedtuple with .success (bool) and .error (str|None)
async def write_power_limit(self, enable: bool, limit_pct: float) -> WriteResult
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ControlState, add OverrideLog and EDPC refresh loop</name>
  <files>src/venus_os_fronius_proxy/control.py, tests/test_control.py</files>
  <read_first>
    - src/venus_os_fronius_proxy/control.py (full file -- extend existing ControlState class)
    - tests/test_control.py (full file -- extend existing test file)
    - .planning/phases/07-power-control/07-RESEARCH.md (Pattern 1, 2, 3 for implementation details)
  </read_first>
  <behavior>
    - ControlState.last_source defaults to "none", set_from_webapp() sets it to "webapp" with revert deadline
    - ControlState.set_from_venus_os() sets last_source="venus_os", cancels webapp_revert_at
    - ControlState.last_change_ts updates on both set_from_webapp and set_from_venus_os
    - OverrideLog(maxlen=50) stores events with ts/source/action/value/detail, auto-evicts oldest
    - OverrideLog.get_all() returns list of dicts, OverrideLog.append() adds event
    - _edpc_refresh_loop re-writes limit when enabled and source != "none"
    - _edpc_refresh_loop auto-reverts when webapp_revert_at deadline passed (disable limit, set source="none", log event)
    - _edpc_refresh_loop does nothing when limit is disabled or source is "none"
  </behavior>
  <action>
    Extend ControlState in control.py:
    1. Add `last_source: str = "none"` (values: "none", "venus_os", "webapp")
    2. Add `last_change_ts: float = 0.0` (time.time() of last change)
    3. Add `webapp_revert_at: float | None = None` (time.monotonic() deadline)
    4. Add `set_from_webapp(raw_value, ena, revert_timeout=300.0)` method -- updates wmaxlimpct, wmaxlim_ena, last_source="webapp", last_change_ts=time.time(), webapp_revert_at=time.monotonic()+revert_timeout
    5. Add `set_from_venus_os()` method -- sets last_source="venus_os", last_change_ts=time.time(), webapp_revert_at=None

    Add OverrideLog class in control.py:
    1. `__init__(maxlen=50)` with collections.deque(maxlen=maxlen)
    2. `append(source, action, value, detail="")` -- appends dict with ts=time.time(), source, action, value, detail
    3. `get_all()` -> list[dict] -- returns list copy of deque

    Add async function `edpc_refresh_loop(plugin, control_state, override_log, interval=30.0, broadcast_fn=None)`:
    1. Infinite loop: await asyncio.sleep(interval)
    2. If not control_state.is_enabled or control_state.last_source == "none": continue
    3. Check auto-revert: if webapp_revert_at is not None and time.monotonic() >= webapp_revert_at, disable limit (plugin.write_power_limit(False, 0.0)), reset control_state (wmaxlim_ena=0, last_source="none", webapp_revert_at=None), log to override_log("system", "revert", None, "auto-revert after timeout"), optionally call broadcast_fn if provided, continue
    4. Otherwise refresh: await plugin.write_power_limit(True, control_state.wmaxlimpct_float), log warning on failure

    Write tests in test_control.py (extend existing file, add new test classes):
    - TestControlStateSourceTracking: test set_from_webapp sets all fields, test set_from_venus_os cancels revert
    - TestOverrideLog: test append and get_all, test maxlen eviction, test empty log
    - TestEdpcRefreshLoop: test refresh writes when active, test auto-revert on deadline, test skip when disabled (use asyncio.wait_for with short interval)
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/test_control.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ControlState has last_source, last_change_ts, webapp_revert_at attributes
    - set_from_webapp and set_from_venus_os methods work correctly
    - OverrideLog stores events with maxlen=50, get_all returns list of dicts
    - edpc_refresh_loop auto-reverts after timeout, refreshes when active, skips when inactive
    - All existing test_control.py tests still pass
    - All new tests pass
  </acceptance_criteria>
  <done>ControlState extended with source tracking, OverrideLog class works, EDPC refresh loop handles refresh and auto-revert, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/power-limit endpoint, Venus OS override in proxy.py, extended dashboard snapshot</name>
  <files>src/venus_os_fronius_proxy/webapp.py, src/venus_os_fronius_proxy/proxy.py, src/venus_os_fronius_proxy/dashboard.py, tests/test_webapp.py, tests/test_proxy.py</files>
  <read_first>
    - src/venus_os_fronius_proxy/webapp.py (full file -- add endpoint and wire OverrideLog)
    - src/venus_os_fronius_proxy/proxy.py (full file -- update _handle_control_write)
    - src/venus_os_fronius_proxy/dashboard.py (full file -- extend collect() control section)
    - tests/test_webapp.py (full file -- existing patterns for test client setup)
    - tests/test_proxy.py (full file -- existing patterns for StalenessAwareSlaveContext tests)
    - .planning/phases/07-power-control/07-RESEARCH.md (Pattern 4, 5 for implementation details)
  </read_first>
  <behavior>
    - POST /api/power-limit with action="set", limit_pct=50.0 calls plugin.write_power_limit(True, 50.0) and returns {"success": true}
    - POST /api/power-limit with action="enable" enables limiting, action="disable" disables it
    - POST /api/power-limit returns 409 when control_state.last_source=="venus_os" and age < 60s
    - POST /api/power-limit returns 400 on invalid limit_pct (negative, >100)
    - POST /api/power-limit response includes WriteResult.success and WriteResult.error for live feedback (CTRL-07)
    - _handle_control_write sets last_source="venus_os", last_change_ts, cancels webapp_revert_at, appends to override_log
    - Dashboard snapshot.control includes last_source, last_change_ts, revert_remaining_s
    - Dashboard snapshot includes override_log key with list of recent events
  </behavior>
  <action>
    **webapp.py:**
    1. Import time, ControlState, OverrideLog, validate_wmaxlimpct from control module
    2. Add `power_limit_handler(request)` async function:
       - Read JSON body: action ("set"|"enable"|"disable"), limit_pct (float, for "set" action)
       - Get control_state, plugin, override_log from shared_ctx / app
       - Venus OS priority check: if control_state.last_source == "venus_os" and (time.time() - control_state.last_change_ts) < 60, return 409 with error message
       - For "set": validate limit_pct (0-100), convert to raw (int(limit_pct * 100)), call validate_wmaxlimpct(raw), call plugin.write_power_limit(True, limit_pct), on success call control_state.set_from_webapp(raw, 1), append to override_log("webapp", "set", limit_pct), return {"success": result.success, "error": result.error}
       - For "enable": call plugin.write_power_limit(True, control_state.wmaxlimpct_float), update state, log
       - For "disable": call plugin.write_power_limit(False, 0.0), control_state.update_wmaxlim_ena(0), control_state.last_source="none", control_state.webapp_revert_at=None, log("webapp", "disable", None)
       - Return JSON with success and error fields in all cases
    3. Register route: `app.router.add_post("/api/power-limit", power_limit_handler)`
    4. Store override_log in shared_ctx (add "override_log" key) -- the caller (run_with_shutdown in proxy.py or main) must create OverrideLog and put it in shared_ctx
    5. In ws_handler read loop: add handling for incoming JSON messages (Phase 7 placeholder comment already exists). Parse msg.data as JSON, but for now no WebSocket commands needed -- POST is the write path.

    **proxy.py:**
    1. Import time at top
    2. In `_handle_control_write`, after successful write operations (both WMaxLimPct and WMaxLim_Ena paths):
       - Set `self._control.last_source = "venus_os"`
       - Set `self._control.last_change_ts = time.time()`
       - Set `self._control.webapp_revert_at = None`
       - If `self._shared_ctx` and `"override_log" in self._shared_ctx`: append event to override_log("venus_os", "set", self._control.wmaxlimpct_float) for WMaxLimPct writes, or ("venus_os", "enable"/"disable", ...) for WMaxLim_Ena writes

    **dashboard.py:**
    1. Import time at top (already imported)
    2. In `collect()`, extend the control section to include:
       - `"last_source": control_state.last_source`
       - `"last_change_ts": control_state.last_change_ts`
       - `"revert_remaining_s": _revert_remaining(control_state)` -- helper function that returns seconds until revert or None
    3. Add `_revert_remaining(control_state)` helper: if webapp_revert_at is not None, return max(0, webapp_revert_at - time.monotonic()), else None
    4. Accept override_log parameter in collect(), add `"override_log": override_log.get_all() if override_log else []` to snapshot

    **Tests:**
    - test_webapp.py: Add tests for power_limit_handler:
      - test_power_limit_set_valid: POST with action="set", limit_pct=50, mock plugin returns success -> 200
      - test_power_limit_set_invalid: limit_pct=150 -> 400
      - test_power_limit_venus_override_rejection: set last_source="venus_os", last_change_ts=recent -> 409
      - test_power_limit_enable_disable: action="enable" and "disable" work
      - test_power_limit_feedback: response includes success/error from WriteResult (CTRL-07)
    - test_proxy.py: Add test for Venus OS override tracking:
      - test_venus_override_sets_source: after _handle_control_write, control.last_source == "venus_os" and webapp_revert_at is None
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/test_webapp.py tests/test_proxy.py tests/test_control.py tests/test_dashboard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/power-limit returns 200 with {"success": true} on valid set/enable/disable
    - POST /api/power-limit returns 409 when Venus OS recently wrote
    - POST /api/power-limit returns 400 on invalid values
    - POST response always includes success and error fields (CTRL-07 live feedback)
    - proxy.py _handle_control_write sets last_source="venus_os" and cancels webapp revert
    - Dashboard snapshot control section includes last_source, last_change_ts, revert_remaining_s
    - Dashboard snapshot includes override_log array
    - All existing tests still pass, all new tests pass
  </acceptance_criteria>
  <done>REST endpoint works with validation and Venus OS priority, proxy tracks override source, dashboard snapshot extended, all tests green</done>
</task>

</tasks>

<verification>
```bash
cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/ -x -q
```
All tests pass including new power control tests.
</verification>

<success_criteria>
- ControlState extended with last_source, last_change_ts, webapp_revert_at, set_from_webapp, set_from_venus_os
- OverrideLog class stores last 50 events
- EDPC refresh loop refreshes every 30s, auto-reverts after 5min
- POST /api/power-limit endpoint with set/enable/disable actions, validation, Venus OS priority (409)
- POST response includes WriteResult success/error for live feedback
- proxy.py tracks Venus OS overrides with source and timestamp
- Dashboard snapshot includes extended control state and override log
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/07-power-control/07-01-SUMMARY.md`
</output>
