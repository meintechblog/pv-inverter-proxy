---
phase: 07-power-control
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/venus_os_fronius_proxy/static/index.html
  - src/venus_os_fronius_proxy/static/app.js
  - src/venus_os_fronius_proxy/static/style.css
autonomous: false
requirements: [CTRL-04, CTRL-05, CTRL-06, CTRL-07, CTRL-08, CTRL-10]

must_haves:
  truths:
    - "Dashboard shows current power limit percentage and source (webapp/venus_os/none)"
    - "User can drag slider 0-100% and see kW equivalent preview without writing"
    - "Clicking Apply opens confirmation dialog showing value; confirming sends POST /api/power-limit"
    - "User can toggle enable/disable with confirmation dialog"
    - "After applying, UI shows success/failure feedback from SE30K (live feedback)"
    - "When Venus OS overrides, status turns red, slider disabled, source shows Venus OS"
    - "Override log displays recent events with timestamp, source, action, value"
    - "Auto-revert countdown timer visible when webapp limit is active"
  artifacts:
    - path: "src/venus_os_fronius_proxy/static/index.html"
      provides: "Power Control nav item and page section with slider, toggle, status, log"
      contains: "page-power"
    - path: "src/venus_os_fronius_proxy/static/app.js"
      provides: "Power control UI logic: slider preview, confirmation dialog, POST calls, override log rendering"
      contains: "applyPowerLimit"
    - path: "src/venus_os_fronius_proxy/static/style.css"
      provides: "Power control component styles: slider, modal, status indicator, log"
      contains: "ve-ctrl"
  key_links:
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "/api/power-limit"
      via: "fetch POST on Apply/Enable/Disable"
      pattern: "fetch.*api/power-limit"
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "WebSocket snapshot"
      via: "handleSnapshot reads data.control for status, slider state, revert countdown"
      pattern: "data\\.control"
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "WebSocket override_event"
      via: "onmessage handler for override_event type triggers toast notification"
      pattern: "override_event"
---

<objective>
Frontend power control page: read-only status display, slider with kW preview and confirmation dialog, enable/disable toggle, live feedback, Venus OS override indication, auto-revert countdown, and scrollable override log.

Purpose: Users can safely test power limiting from the webapp with visual feedback and safety confirmations.
Output: Complete Power Control page in the dashboard frontend.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-power-control/07-CONTEXT.md
@.planning/phases/07-power-control/07-RESEARCH.md
@.planning/phases/07-power-control/07-01-SUMMARY.md

<interfaces>
<!-- Backend contracts from Plan 01. Executor needs these to build the frontend. -->

POST /api/power-limit request/response:
```
Request:  {"action": "set", "limit_pct": 50.0}
          {"action": "enable"}
          {"action": "disable"}
Response: {"success": true, "error": null}
          {"success": false, "error": "Venus OS is currently controlling power limit"} (409)
          {"success": false, "error": "Invalid value: exceeds 100%"} (400)
```

WebSocket snapshot.control object (pushed every poll cycle):
```json
{
  "enabled": true,
  "limit_pct": 50.0,
  "wmaxlimpct_raw": 5000,
  "last_source": "webapp",
  "last_change_ts": 1710770400.0,
  "revert_remaining_s": 245.3
}
```

WebSocket snapshot.override_log (array of recent events):
```json
[
  {"ts": 1710770400.0, "source": "webapp", "action": "set", "value": 50.0, "detail": ""},
  {"ts": 1710770100.0, "source": "venus_os", "action": "set", "value": 30.0, "detail": ""},
  {"ts": 1710770000.0, "source": "system", "action": "revert", "value": null, "detail": "auto-revert after timeout"}
]
```

Existing CSS variables (from style.css):
```css
--ve-blue: #387DC5;
--ve-bg: #141414;
--ve-surface: #1E1E1E;
--ve-text: #FAF9F5;
--ve-text-dim: #999;
--ve-green: #4CAF50;
--ve-orange: #FF9800;
--ve-red: #F44336;
--ve-border: #333;
```

Existing nav pattern (from index.html):
```html
<a class="nav-item" data-page="config">...</a>
<!-- pages use id="page-{name}" class="page" -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Power Control page HTML, CSS, and JS</name>
  <files>src/venus_os_fronius_proxy/static/index.html, src/venus_os_fronius_proxy/static/style.css, src/venus_os_fronius_proxy/static/app.js</files>
  <read_first>
    - src/venus_os_fronius_proxy/static/index.html (full file -- add nav item + page section)
    - src/venus_os_fronius_proxy/static/style.css (full file -- add power control styles)
    - src/venus_os_fronius_proxy/static/app.js (full file -- add power control logic)
    - .planning/phases/07-power-control/07-RESEARCH.md (Frontend code examples for slider, confirmation, log)
    - .planning/phases/07-power-control/07-CONTEXT.md (UI decisions, safety requirements)
  </read_first>
  <action>
    **index.html -- add Power Control nav + page:**
    1. Add nav item after "Registers" link:
       ```html
       <a class="nav-item" data-page="power">
         <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
           <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/>
         </svg>
         <span class="nav-label">Power Control</span>
       </a>
       ```
    2. Add page section after page-registers div. Use existing ve-card/ve-panel patterns. Structure:
       - Status card: "Power Control" title with `<span class="ve-badge ve-badge--test">Manual Test</span>`, color-coded status dot (green=no limit, orange=limited, red=Venus OS override), readout showing current limit %, source, last changed time
       - Slider group (inside same card): range input 0-100, kW equivalent label (e.g. "50% = 15.0 kW"), Apply button (ve-btn ve-btn--primary), Enable/Disable toggle button
       - Revert countdown: "Auto-revert in: 4:32" (hidden when no webapp limit active)
       - Venus OS override banner: shown when last_source=="venus_os", red background, "Venus OS has control -- manual slider disabled"
       - Override Log card: scrollable list of recent events (timestamp, source badge, action, value)

    **style.css -- add power control component styles:**
    1. `.ve-ctrl-status` -- flex row with dot and label
    2. `.ve-ctrl-readout` -- grid layout for limit/source/timestamp
    3. `.ve-ctrl-slider` -- styled range input matching Venus OS theme (track: --ve-border, thumb: --ve-blue)
    4. `.ve-ctrl-slider-group` -- container with slider, labels, buttons
    5. `.ve-ctrl-slider-labels` -- flex row for 0% / value / 100%
    6. `.ve-ctrl-revert` -- countdown display with warning color
    7. `.ve-ctrl-override-banner` -- red banner for Venus OS override state
    8. `.ve-modal-overlay` -- full-screen overlay (position: fixed, rgba backdrop)
    9. `.ve-modal` -- centered dialog box (--ve-surface background, rounded corners)
    10. `.ve-modal-body` -- padding, text styling
    11. `.ve-modal-actions` -- flex row for Cancel/Confirm buttons
    12. `.ve-btn--danger` -- red confirmation button (--ve-red background)
    13. `.ve-badge--test` -- small badge "Manual Test" (--ve-orange background, small font)
    14. `.ve-override-log` -- scrollable container (max-height: 300px, overflow-y: auto)
    15. `.ve-log-entry` -- single log row with timestamp, source badge, action, value
    16. `.ve-source-badge` -- colored badge for source (webapp=blue, venus_os=red, system=orange)
    17. `.ve-toast` -- fixed position notification (top-right, slides in)

    **app.js -- add power control logic (append to existing file):**
    1. Constants: `const RATED_KW = 30;`
    2. `showConfirmDialog(message, onConfirm)` -- creates overlay + modal with Cancel/Confirm buttons, Confirm calls onConfirm and removes overlay, Cancel just removes overlay. Escape key also closes.
    3. `showToast(message, type)` -- creates toast notification (success=green, error=red, info=blue), auto-removes after 3s
    4. Slider input handler: on `input` event, update kW preview label (`pct% = X.X kW`). NO write on input -- preview only.
    5. Apply button click handler: read slider value, call `showConfirmDialog("Set power limit to <strong>X% (Y.Y kW)</strong>?<br>This limit will auto-revert after 5 minutes.", () => applyPowerLimit(pct))`
    6. `async applyPowerLimit(pct)` -- POST /api/power-limit with action="set", limit_pct=pct. On success: showToast("Power limit set to X%", "success"). On 409: showToast("Venus OS is controlling -- manual override blocked", "error"). On other error: showToast(error message, "error").
    7. Enable/Disable toggle handler: determine current state from last snapshot, show confirmation, POST action="enable" or "disable"
    8. Extend `handleSnapshot(data)` to update power control section:
       - Read data.control (last_source, limit_pct, enabled, revert_remaining_s)
       - Update status dot color: green if !enabled or source=="none", orange if source=="webapp", red if source=="venus_os"
       - Update readout: limit %, source display name, timestamp (format last_change_ts as relative time "2m ago" or absolute)
       - Update slider position and kW label to match server state (but only if user is not actively dragging)
       - Show/hide revert countdown (format revert_remaining_s as M:SS)
       - Show/hide Venus OS override banner (disable slider + apply button when source=="venus_os")
       - Update enable/disable toggle button text
    9. Add override log rendering: `updateOverrideLog(events)` function
       - Receives array from data.override_log
       - For each event: create log entry with formatted timestamp, colored source badge, action text, value
       - Show "No events yet" when empty
       - Newest events at top
    10. Handle `override_event` WebSocket message type: call showToast with event details, update log

    **Critical safety rules (per CONTEXT.md locked decisions):**
    - Slider NEVER sends writes on drag/input -- only preview
    - Apply button ALWAYS shows confirmation dialog first
    - Enable/Disable toggle ALWAYS shows confirmation dialog first
    - When Venus OS has control: slider disabled, Apply disabled, banner visible
    - "Manual Test" badge always visible in Power Control header
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -c "
from venus_os_fronius_proxy.static import __path__ as sp
import importlib.resources as r
# Verify HTML has power control page
html = (r.files('venus_os_fronius_proxy') / 'static' / 'index.html').read_text()
assert 'page-power' in html, 'Missing power control page'
assert 'Manual Test' in html, 'Missing Manual Test badge'
assert 'ctrl-slider' in html, 'Missing slider'
assert 'override-log' in html, 'Missing override log'
# Verify JS has power control logic
js = (r.files('venus_os_fronius_proxy') / 'static' / 'app.js').read_text()
assert 'applyPowerLimit' in js, 'Missing applyPowerLimit function'
assert 'showConfirmDialog' in js, 'Missing confirmation dialog'
assert 'api/power-limit' in js, 'Missing API call'
assert 'override_event' in js, 'Missing override event handler'
# Verify CSS has power control styles
css = (r.files('venus_os_fronius_proxy') / 'static' / 'style.css').read_text()
assert 've-ctrl' in css, 'Missing power control styles'
assert 've-modal' in css, 'Missing modal styles'
print('All power control frontend checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - Power Control nav item visible in sidebar with lightning bolt icon
    - Power Control page has status display, slider, Apply button, Enable/Disable toggle, revert countdown, override log
    - Slider updates kW preview on drag without sending any writes
    - Apply button triggers confirmation dialog before POST
    - Enable/Disable toggle triggers confirmation dialog before POST
    - handleSnapshot updates power control status from data.control
    - Venus OS override disables slider and shows red banner
    - Override log renders events with colored source badges
    - Toast notifications on apply success/failure
    - "Manual Test" badge visible in header
    - All existing dashboard, config, register functionality unchanged
  </acceptance_criteria>
  <done>Power Control page fully functional with slider, confirmation, toggle, live status, Venus OS override indication, revert countdown, and override log</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Power Control UI</name>
  <what-built>Complete Power Control page with safety confirmations, Venus OS override display, and override log. Backend endpoints, EDPC refresh loop, and auto-revert are also wired.</what-built>
  <how-to-verify>
    1. Open webapp in browser (http://192.168.3.20:8080 or local dev)
    2. Click "Power Control" in sidebar -- page should load with "Manual Test" badge
    3. Verify read-only display shows current limit state (likely "No limit active" / source "none")
    4. Drag slider -- verify kW preview updates (e.g., "50% = 15.0 kW") but NO write happens
    5. Click "Apply" -- confirmation dialog should appear with value preview and revert warning
    6. Click "Cancel" -- dialog closes, no change
    7. Click "Apply" again, then "Confirm" -- should POST to API, show success toast
    8. Verify status updates: dot turns orange, source shows "webapp", revert countdown visible
    9. Click "Disable" -- confirmation dialog, confirm, limit disabled
    10. Check Override Log -- should show the set and disable events with timestamps
    11. Navigate to Dashboard, Config, Registers tabs -- all should still work normally
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All backend tests pass: `cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/ -x -q`
2. Frontend contains all required elements (power page, slider, confirmation, log)
3. Power Control page loads and functions in browser
4. Safety confirmations work (dialog before every write)
5. Venus OS override indication functional
</verification>

<success_criteria>
- Power Control page accessible from sidebar navigation
- Read-only display shows limit %, source, timestamp (CTRL-04)
- Slider 0-100% with kW preview, Apply confirmation dialog (CTRL-05)
- Enable/Disable toggle with confirmation (CTRL-06)
- Live feedback: success/failure toast after SE30K response (CTRL-07)
- Venus OS override: red status, disabled slider, banner (CTRL-08)
- Override log: scrollable list of events (CTRL-10)
- "Manual Test" badge clearly labels webapp control as test mode
- All existing dashboard/config/register pages unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/07-power-control/07-02-SUMMARY.md`
</output>
