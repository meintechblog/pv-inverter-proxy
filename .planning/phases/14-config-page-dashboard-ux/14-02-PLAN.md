---
phase: 14-config-page-dashboard-ux
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/venus_os_fronius_proxy/static/index.html
  - src/venus_os_fronius_proxy/static/app.js
  - src/venus_os_fronius_proxy/static/style.css
autonomous: false
requirements: [CFG-01, CFG-02, SETUP-02, SETUP-03]

must_haves:
  truths:
    - "Config page shows Venus OS Configuration section with IP, MQTT Port, Portal ID fields below inverter section"
    - "Config page shows pre-filled defaults (empty venus fields with placeholders) on first visit"
    - "Connection bobbles next to section headings show live green/red/amber/grey status"
    - "Test Connection button is removed from config page"
    - "Save & Apply sends nested {inverter, venus} payload and shows toast feedback"
    - "Dashboard Venus-dependent widgets (lock toggle, ESS panel) are greyed out when MQTT disconnected"
    - "Dashboard inverter-only widgets (gauge, slider, phases, performance) remain functional without MQTT"
    - "MQTT setup guide card appears on config page when venus host configured but MQTT disconnected"
  artifacts:
    - path: "src/venus_os_fronius_proxy/static/index.html"
      provides: "Venus OS config section, connection bobble markup, MQTT setup guide card HTML, venus-dependent class on gated elements"
      contains: "Venus OS Configuration"
    - path: "src/venus_os_fronius_proxy/static/app.js"
      provides: "Updated loadConfig, saveConfig for nested format, updateMqttGate, connection bobble updates, setup guide visibility logic"
      contains: "updateMqttGate"
    - path: "src/venus_os_fronius_proxy/static/style.css"
      provides: "MQTT gate CSS, connection bobble pulse animation, hint card styling"
      contains: "mqtt-gated"
  key_links:
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "/api/config"
      via: "fetch in loadConfig and form submit"
      pattern: "data\\.inverter\\."
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "WebSocket snapshot"
      via: "venus_mqtt_connected in handleSnapshot"
      pattern: "venus_mqtt_connected"
    - from: "src/venus_os_fronius_proxy/static/index.html"
      to: "src/venus_os_fronius_proxy/static/app.js"
      via: "venus-dependent class on gated containers"
      pattern: "venus-dependent"
---

<objective>
Build the frontend: Venus OS config section with connection bobbles, MQTT setup guide card, dashboard MQTT gate, and updated config form to use the new nested API format from Plan 01.

Purpose: Users can configure Venus OS MQTT from the web UI, see live connection status, and understand which dashboard features require MQTT.
Output: Updated index.html, app.js, style.css with all config page and dashboard MQTT gate features.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-config-page-dashboard-ux/14-RESEARCH.md
@.planning/phases/14-config-page-dashboard-ux/14-UI-SPEC.md
@.planning/phases/14-config-page-dashboard-ux/14-01-SUMMARY.md

<interfaces>
<!-- API contract from Plan 01 -->

GET /api/config response:
```json
{
  "inverter": {"host": "192.168.3.18", "port": 1502, "unit_id": 1},
  "venus": {"host": "", "port": 1883, "portal_id": ""}
}
```

POST /api/config request body:
```json
{
  "inverter": {"host": "192.168.3.18", "port": 1502, "unit_id": 1},
  "venus": {"host": "10.0.0.5", "port": 1883, "portal_id": ""}
}
```

GET /api/status response:
```json
{"solaredge": "connected", "venus_os": "connected|disconnected|not configured", "reconfiguring": false}
```

WebSocket snapshot includes:
```json
{
  "venus_mqtt_connected": true|false,
  "connection": {"state": "connected|reconnecting|disconnected", ...}
}
```

Existing CSS custom properties (from style.css :root):
- --ve-green: #72B84C
- --ve-orange: #F0962E
- --ve-red: #F35C58
- --ve-border: #64635F
- --ve-text-dim: #969591
- --ve-bg-surface: #272622
- --ve-bg-widget: #11263B
- --ve-duration-normal: 300ms
- --ve-easing-default: cubic-bezier(0.4, 0, 0.2, 1)

Existing HTML IDs for MQTT gate targets:
- #venus-lock-container (Venus OS Control toggle in gauge card)
- #venus-ess-panel (Venus OS ESS card)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Venus OS config section + connection bobbles + updated config form JS</name>
  <files>src/venus_os_fronius_proxy/static/index.html, src/venus_os_fronius_proxy/static/app.js, src/venus_os_fronius_proxy/static/style.css</files>
  <read_first>
    - src/venus_os_fronius_proxy/static/index.html (full file -- config page at line 252, dashboard at line 60)
    - src/venus_os_fronius_proxy/static/app.js (loadConfig at line 591, Test Connection at line 603, Save Config at line 632, updateConnectionStatus at line 492, handleSnapshot at line 110)
    - src/venus_os_fronius_proxy/static/style.css (full file -- existing ve-panel, ve-form-group, ve-input, ve-dot classes, :root variables)
    - .planning/phases/14-config-page-dashboard-ux/14-UI-SPEC.md (visual specs for all new components)
  </read_first>
  <action>
    **HTML changes (index.html):**

    1. Add `venus-dependent` class to the gated dashboard containers:
       - On the `<div class="ve-gauge-venus-toggle">` wrapper (contains `#venus-lock-container`): add class `venus-dependent`
       - On `<div class="ve-card" id="venus-ess-panel">`: add class `venus-dependent`

    2. In the config page (`#page-config`), modify the existing Inverter Configuration panel heading to include a connection bobble:
       ```html
       <h2><span class="ve-dot ve-dot--dim" id="cfg-se-dot"></span> SolarEdge Inverter</h2>
       ```
       The dot is 12px inline with the heading text.

    3. After the closing `</div>` of the Inverter Configuration `ve-panel` (line ~275), add a new Venus OS Configuration panel:
       ```html
       <div class="ve-panel">
         <h2><span class="ve-dot ve-dot--dim" id="cfg-venus-dot"></span> Venus OS Configuration</h2>
         <div class="ve-form-group">
           <label for="venus-host">Venus OS IP</label>
           <input type="text" id="venus-host" class="ve-input" placeholder="e.g. 192.168.1.1">
         </div>
         <div class="ve-form-group">
           <label for="venus-port">MQTT Port</label>
           <input type="number" id="venus-port" class="ve-input" placeholder="1883" min="1" max="65535">
         </div>
         <div class="ve-form-group">
           <label for="venus-portal-id">Portal ID</label>
           <input type="text" id="venus-portal-id" class="ve-input" placeholder="leave blank for auto-discovery">
         </div>
       </div>
       ```

    4. After the Venus OS Configuration panel, add the MQTT setup guide card:
       ```html
       <div id="mqtt-setup-guide" class="ve-hint-card" style="display:none">
         <div class="ve-hint-header">
           <svg width="20" height="20" viewBox="0 0 20 20" fill="none" style="flex-shrink:0">
             <circle cx="10" cy="10" r="9" stroke="currentColor" stroke-width="1.5" fill="none"/>
             <path d="M10 9v5M10 6.5v.01" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
           </svg>
           <h3>Enable MQTT on Venus OS</h3>
         </div>
         <ol>
           <li>Open Venus OS Remote Console</li>
           <li>Go to Settings &gt; Services</li>
           <li>Enable "MQTT on LAN"</li>
           <li>Return here and click Save &amp; Apply</li>
         </ol>
         <p class="ve-hint-subtext">MQTT allows the proxy to read grid data and control ESS settings on your Venus OS device.</p>
       </div>
       ```

    5. Remove the "Test Connection" button from the button group. Change:
       ```html
       <div class="ve-button-group">
         <button type="button" id="btn-test" class="ve-btn">Test Connection</button>
         <button type="submit" id="btn-save" class="ve-btn ve-btn--primary">Save &amp; Apply</button>
       </div>
       ```
       To:
       ```html
       <div class="ve-button-group">
         <button type="submit" id="btn-save" class="ve-btn ve-btn--primary">Save &amp; Apply</button>
       </div>
       ```

    6. Move the `<form id="config-form">` to wrap BOTH the inverter panel and venus panel, with the Save & Apply button after the venus panel. The button group and config-message div should be after the venus panel but still inside the form.

    **CSS changes (style.css):**

    1. Add connection bobble pulse animation:
       ```css
       @keyframes ve-pulse {
           0%, 100% { opacity: 1; }
           50% { opacity: 0.5; }
       }
       .ve-dot--ok { animation: ve-pulse 2s ease-in-out infinite; }
       .ve-dot--warn { animation: ve-pulse 1.5s ease-in-out infinite; }
       ```
       NOTE: Check if `.ve-dot--ok` already has styles. If so, ADD the animation property to the existing rule, do NOT create a duplicate.

    2. Add MQTT gate CSS:
       ```css
       .mqtt-gated {
           position: relative;
           opacity: 0.35;
           pointer-events: none;
           transition: opacity var(--ve-duration-normal) var(--ve-easing-default);
       }
       .mqtt-gated::after {
           content: 'Requires Venus OS MQTT';
           position: absolute;
           top: 50%;
           left: 50%;
           transform: translate(-50%, -50%);
           color: var(--ve-text-dim);
           font-size: 0.85rem;
           text-align: center;
           white-space: nowrap;
           pointer-events: none;
       }
       ```

    3. Add hint card CSS:
       ```css
       .ve-hint-card {
           background: rgba(240, 150, 46, 0.12);
           border: 1px solid rgba(240, 150, 46, 0.25);
           border-radius: 12px;
           padding: 16px;
           margin-top: 16px;
       }
       .ve-hint-header {
           display: flex;
           align-items: center;
           gap: 8px;
           color: var(--ve-orange);
           margin-bottom: 12px;
       }
       .ve-hint-header h3 {
           margin: 0;
           font-size: 1em;
           font-weight: 600;
       }
       .ve-hint-card ol {
           margin: 0 0 12px 20px;
           padding: 0;
           color: var(--ve-text);
           font-size: 0.9em;
           line-height: 1.8;
       }
       .ve-hint-subtext {
           margin: 0;
           font-size: 0.85em;
           color: var(--ve-text-dim);
       }
       ```

    4. Add inline dot styling in panel headings:
       ```css
       .ve-panel h2 .ve-dot,
       .ve-panel h2 > .ve-dot {
           display: inline-block;
           vertical-align: middle;
           margin-right: 8px;
       }
       ```

    5. Add `prefers-reduced-motion` override for ve-pulse:
       ```css
       @media (prefers-reduced-motion: reduce) {
           .ve-dot--ok, .ve-dot--warn { animation: none; }
       }
       ```
       This should go inside the existing `@media (prefers-reduced-motion: reduce)` block if one exists.

    **JavaScript changes (app.js):**

    1. **Update loadConfig()** to read nested format:
       ```javascript
       async function loadConfig() {
           try {
               var res = await fetch('/api/config');
               var data = await res.json();
               document.getElementById('se-host').value = data.inverter.host;
               document.getElementById('se-port').value = data.inverter.port;
               document.getElementById('se-unit').value = data.inverter.unit_id;
               document.getElementById('venus-host').value = data.venus.host;
               document.getElementById('venus-port').value = data.venus.port;
               document.getElementById('venus-portal-id').value = data.venus.portal_id;
           } catch (e) {
               console.error('Config load failed:', e);
           }
       }
       ```

    2. **Remove the entire Test Connection event listener** (lines ~605-630, the `document.getElementById('btn-test').addEventListener(...)` block).

    3. **Update Save Config form submit handler** to send nested format:
       ```javascript
       body: JSON.stringify({
           inverter: {
               host: document.getElementById('se-host').value,
               port: parseInt(document.getElementById('se-port').value),
               unit_id: parseInt(document.getElementById('se-unit').value)
           },
           venus: {
               host: document.getElementById('venus-host').value.trim(),
               port: parseInt(document.getElementById('venus-port').value) || 1883,
               portal_id: document.getElementById('venus-portal-id').value.trim()
           }
       })
       ```
       On success, call `showToast('Configuration saved. Reconnecting...', 'success')`.
       On failure, call `showToast('Save failed: ' + data.error, 'error')`.
       During save, set btn-save text to "Saving..." and disabled=true, revert after response.

    4. **Add updateMqttGate(snapshot) function:**
       ```javascript
       function updateMqttGate(snapshot) {
           var mqttConnected = snapshot.venus_mqtt_connected;
           var gatedEls = document.querySelectorAll('.venus-dependent');
           for (var i = 0; i < gatedEls.length; i++) {
               if (mqttConnected) {
                   gatedEls[i].classList.remove('mqtt-gated');
                   gatedEls[i].removeAttribute('aria-disabled');
               } else {
                   gatedEls[i].classList.add('mqtt-gated');
                   gatedEls[i].setAttribute('aria-disabled', 'true');
               }
           }
       }
       ```
       Call `updateMqttGate(data)` at the end of `handleSnapshot(data)`.

    5. **Add updateConfigBobbles(snapshot) function:**
       ```javascript
       function updateConfigBobbles(snapshot) {
           // SolarEdge bobble on config page
           var seDot = document.getElementById('cfg-se-dot');
           if (seDot) {
               var seState = snapshot.connection ? snapshot.connection.state : 'disconnected';
               seDot.className = 've-dot ' + (seState === 'connected' ? 've-dot--ok' : seState === 'reconnecting' ? 've-dot--warn' : 've-dot--err');
           }
           // Venus OS bobble on config page
           var venusDot = document.getElementById('cfg-venus-dot');
           if (venusDot) {
               var venusHost = document.getElementById('venus-host');
               var hostConfigured = venusHost && venusHost.value.trim() !== '';
               if (!hostConfigured) {
                   venusDot.className = 've-dot ve-dot--dim';
               } else if (snapshot.venus_mqtt_connected) {
                   venusDot.className = 've-dot ve-dot--ok';
               } else {
                   venusDot.className = 've-dot ve-dot--err';
               }
           }
       }
       ```
       Call `updateConfigBobbles(data)` at the end of `handleSnapshot(data)`.

    6. **Add updateSetupGuide(snapshot) function:**
       ```javascript
       function updateSetupGuide(snapshot) {
           var guide = document.getElementById('mqtt-setup-guide');
           if (!guide) return;
           var venusHost = document.getElementById('venus-host');
           var hostConfigured = venusHost && venusHost.value.trim() !== '';
           if (!snapshot.venus_mqtt_connected && hostConfigured) {
               guide.style.display = '';
           } else {
               guide.style.display = 'none';
           }
       }
       ```
       Call `updateSetupGuide(data)` at the end of `handleSnapshot(data)`.

    7. **Update updateConnectionStatus(conn)** (the function that polls /api/status) to use the new venus_os status values from Plan 01:
       Change the Venus OS dot/label logic from checking `data.venus_os === 'active'` to:
       ```javascript
       if (data.venus_os === 'connected') {
           vosDotMod = 've-dot--ok';
           vosText = 'Venus OS: Connected';
       } else if (data.venus_os === 'disconnected') {
           vosDotMod = 've-dot--err';
           vosText = 'Venus OS: Disconnected';
       } else {
           vosDotMod = 've-dot--dim';
           vosText = 'Venus OS: Not Configured';
       }
       ```
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/ -x -q --timeout=10 && grep -q "venus-dependent" src/venus_os_fronius_proxy/static/index.html && grep -q "mqtt-gated" src/venus_os_fronius_proxy/static/style.css && grep -q "updateMqttGate" src/venus_os_fronius_proxy/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'Venus OS Configuration' src/venus_os_fronius_proxy/static/index.html
    - grep -q 'venus-host' src/venus_os_fronius_proxy/static/index.html
    - grep -q 'venus-port' src/venus_os_fronius_proxy/static/index.html
    - grep -q 'venus-portal-id' src/venus_os_fronius_proxy/static/index.html
    - grep -q 'cfg-se-dot' src/venus_os_fronius_proxy/static/index.html
    - grep -q 'cfg-venus-dot' src/venus_os_fronius_proxy/static/index.html
    - grep -q 'mqtt-setup-guide' src/venus_os_fronius_proxy/static/index.html
    - grep -q 'venus-dependent' src/venus_os_fronius_proxy/static/index.html
    - grep -c 'btn-test' src/venus_os_fronius_proxy/static/index.html returns 0 (Test Connection button removed)
    - grep -q 'mqtt-gated' src/venus_os_fronius_proxy/static/style.css
    - grep -q 've-pulse' src/venus_os_fronius_proxy/static/style.css
    - grep -q 've-hint-card' src/venus_os_fronius_proxy/static/style.css
    - grep -q 'updateMqttGate' src/venus_os_fronius_proxy/static/app.js
    - grep -q 'updateConfigBobbles' src/venus_os_fronius_proxy/static/app.js
    - grep -q 'updateSetupGuide' src/venus_os_fronius_proxy/static/app.js
    - grep -q 'data.inverter.host' src/venus_os_fronius_proxy/static/app.js (nested format in loadConfig)
    - grep -q 'data.venus.host' src/venus_os_fronius_proxy/static/app.js (venus fields in loadConfig)
    - grep -qv 'btn-test' src/venus_os_fronius_proxy/static/app.js confirms Test Connection listener removed (no getElementById btn-test)
    - python -m pytest tests/ -x -q --timeout=10 exits 0
  </acceptance_criteria>
  <done>
    Config page shows Venus OS Configuration section with IP, MQTT Port, Portal ID fields.
    Connection bobbles appear next to each config section heading.
    Test Connection button is removed.
    Save & Apply sends nested payload with toast feedback.
    MQTT setup guide card appears when venus configured but disconnected.
    Dashboard MQTT gate greys out venus-dependent widgets.
    All tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of config page and MQTT gate</name>
  <files>src/venus_os_fronius_proxy/static/index.html, src/venus_os_fronius_proxy/static/app.js, src/venus_os_fronius_proxy/static/style.css</files>
  <read_first>
    - src/venus_os_fronius_proxy/static/index.html (verify Venus OS section rendered)
  </read_first>
  <action>
    This is a human verification checkpoint. No code changes needed. The user visually verifies the config page and dashboard MQTT gate behavior in a browser.
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/ -x -q --timeout=10</automated>
  </verify>
  <what-built>
    Complete config page with Venus OS fields, connection bobbles, MQTT setup guide card, and dashboard MQTT gate on Venus-dependent widgets. The Test Connection button has been removed and replaced by live connection bobbles.
  </what-built>
  <how-to-verify>
    1. Open the webapp in a browser (default: http://localhost:80 or the configured port)
    2. Navigate to the Config page via sidebar
    3. Verify: SolarEdge section has a colored dot next to "SolarEdge Inverter" heading (green if connected, red if not)
    4. Verify: Venus OS Configuration section appears below with IP, MQTT Port, Portal ID fields
    5. Verify: Venus OS dot is grey (not configured) when host field is empty
    6. Verify: "Test Connection" button is gone -- only "Save & Apply" button remains
    7. Verify: Portal ID field has placeholder "leave blank for auto-discovery"
    8. Enter a Venus OS IP (e.g. your Venus OS device IP) and click Save & Apply
    9. Verify: Toast appears "Configuration saved. Reconnecting..."
    10. Verify: If Venus OS MQTT is reachable, the Venus bobble turns green
    11. Verify: If Venus OS MQTT is NOT reachable, the Venus bobble stays red and the MQTT setup guide card appears below with 4-step instructions
    12. Navigate to Dashboard page
    13. Verify: Power gauge, phase cards, power slider, inverter status all work normally
    14. Verify: If MQTT is disconnected, the Venus OS Control toggle and Venus OS ESS card are greyed out with "Requires Venus OS MQTT" overlay text
    15. Verify: If MQTT is connected, the Venus OS Control toggle and ESS panel are fully interactive
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <done>User has visually verified config page layout, connection bobbles, MQTT setup guide, and dashboard MQTT gate behavior.</done>
</task>

</tasks>

<verification>
python -m pytest tests/ -x -v --timeout=10
Visual inspection: config page layout, bobble colors, MQTT gate, setup guide
</verification>

<success_criteria>
- Config page shows Venus OS Configuration section with 3 fields and placeholders
- Connection bobbles reflect live SolarEdge and Venus OS connection state
- Test Connection button is removed
- Save & Apply sends nested payload and shows toast
- MQTT setup guide card shown when configured but disconnected
- Dashboard Venus-dependent widgets greyed out when MQTT disconnected
- Inverter-only widgets remain fully functional without MQTT
- All backend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-config-page-dashboard-ux/14-02-SUMMARY.md`
</output>
