---
phase: 05-data-pipeline-theme-foundation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/venus_os_fronius_proxy/static/index.html
  - src/venus_os_fronius_proxy/static/style.css
  - src/venus_os_fronius_proxy/static/app.js
  - tests/test_theme.py
autonomous: false
requirements: [DASH-01, INFRA-04]

must_haves:
  truths:
    - "Opening the webapp shows a Venus OS themed dark UI with correct blue (#387DC5) accent and dark (#141414) background"
    - "Sidebar navigation has 3 items: Dashboard, Config, Registers with icons"
    - "Clicking sidebar items switches visible page content without page reload"
    - "Config form loads current config, tests connection, saves settings (same as v1.0)"
    - "Register viewer shows side-by-side SE30K/Fronius values with collapsible models (same as v1.0)"
    - "Status dots and health metrics update every 2 seconds (same as v1.0)"
    - "Layout is responsive: expanded sidebar on desktop, icon-only on tablet, hamburger on mobile"
    - "index.html loads style.css and app.js from /static/ paths"
  artifacts:
    - path: "src/venus_os_fronius_proxy/static/index.html"
      provides: "App shell with sidebar, header, and page containers"
      contains: "/static/style.css"
    - path: "src/venus_os_fronius_proxy/static/style.css"
      provides: "Venus OS themed CSS with custom properties"
      contains: "--ve-blue: #387DC5"
    - path: "src/venus_os_fronius_proxy/static/app.js"
      provides: "Frontend logic: navigation, polling, config form, register viewer"
      contains: "pollStatus"
    - path: "tests/test_theme.py"
      provides: "Smoke tests verifying CSS colors and HTML references"
  key_links:
    - from: "src/venus_os_fronius_proxy/static/index.html"
      to: "/static/style.css"
      via: "link rel=stylesheet href"
      pattern: "href.*static/style\\.css"
    - from: "src/venus_os_fronius_proxy/static/index.html"
      to: "/static/app.js"
      via: "script src"
      pattern: "src.*static/app\\.js"
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "/api/status"
      via: "fetch calls for status polling"
      pattern: "fetch.*api/status"
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "/api/registers"
      via: "fetch calls for register viewer"
      pattern: "fetch.*api/registers"
---

<objective>
Rewrite the frontend into a 3-file split (index.html + style.css + app.js) with Venus OS visual identity, sidebar navigation, and all existing functionality ported to the new layout.

Purpose: Establishes the visual foundation and multi-page navigation that Phases 6-8 build upon. The dashboard page is a placeholder shell that Phase 6 fills with live widgets.

Output: Rewritten `index.html`, new `style.css`, new `app.js`, smoke tests for theme.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-pipeline-theme-foundation/05-CONTEXT.md
@.planning/phases/05-data-pipeline-theme-foundation/05-RESEARCH.md
@.planning/phases/05-data-pipeline-theme-foundation/05-01-SUMMARY.md

@src/venus_os_fronius_proxy/static/index.html
@src/venus_os_fronius_proxy/webapp.py

<interfaces>
<!-- Plan 01 provides these, available when Plan 02 executes -->

From webapp.py (after Plan 01):
```
Routes available:
  GET /              -> index_handler (serves index.html)
  GET /static/{filename} -> static_handler (serves .css, .js with correct Content-Type)
  GET /api/status    -> JSON {solaredge, venus_os, reconfiguring}
  GET /api/health    -> JSON {uptime_seconds, poll_success_rate, poll_total, poll_success, cache_stale, last_poll_age}
  GET /api/config    -> JSON {host, port, unit_id}
  POST /api/config   -> JSON {success, error?}
  POST /api/config/test -> JSON {success, error?}
  GET /api/registers -> JSON [model objects with fields]
  GET /api/dashboard -> JSON snapshot (from DashboardCollector) or {error} 503
```

Static file serving:
- `/static/style.css` served with `text/css`
- `/static/app.js` served with `application/javascript`
- Files loaded via `importlib.resources.files("venus_os_fronius_proxy") / "static" / filename`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Venus OS themed style.css and rewrite index.html with sidebar layout</name>
  <files>
    src/venus_os_fronius_proxy/static/style.css,
    src/venus_os_fronius_proxy/static/index.html,
    tests/test_theme.py
  </files>
  <read_first>
    src/venus_os_fronius_proxy/static/index.html,
    .planning/phases/05-data-pipeline-theme-foundation/05-RESEARCH.md,
    .planning/phases/05-data-pipeline-theme-foundation/05-CONTEXT.md
  </read_first>
  <action>
    **style.css** -- Create `src/venus_os_fronius_proxy/static/style.css` with:

    CSS custom properties (EXACT values from Venus OS gui-v2 Dark.json per locked decision):
    ```css
    :root {
        --ve-blue: #387DC5;
        --ve-blue-light: #73A2D3;
        --ve-blue-dim: #27588A;
        --ve-orange: #F0962E;
        --ve-red: #F35C58;
        --ve-green: #72B84C;
        --ve-bg: #141414;
        --ve-bg-surface: #272622;
        --ve-bg-widget: #11263B;
        --ve-border: #64635F;
        --ve-text: #FAF9F5;
        --ve-text-dim: #969591;
        --ve-text-secondary: #DCDBD7;
        --ve-ok: var(--ve-blue);
        --ve-warning: var(--ve-orange);
        --ve-error: var(--ve-red);
        --ve-success: var(--ve-green);
        --ve-mono: 'Courier New', monospace;
        --ve-font: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }
    ```

    Layout classes (use ONLY `.ve-` prefixed classes to avoid conflicts per anti-pattern in RESEARCH.md):
    - `.app-shell` -- CSS Grid: `grid-template-columns: 220px 1fr`, `min-height: 100vh`
    - `.sidebar` -- `background: var(--ve-bg-surface)`, `border-right: 1px solid var(--ve-border)`, flex column
    - `.sidebar-header` -- logo area with proxy branding icon (SVG solar panel icon, inline)
    - `.nav-item` -- sidebar link with hover state using `var(--ve-blue)`
    - `.nav-item.active` -- highlighted with `background: var(--ve-blue-dim)`
    - `.nav-icon` -- 20x20 inline SVG icon
    - `.nav-label` -- text label hidden on tablet/mobile
    - `.content` -- `overflow-y: auto`, flex-grow
    - `.top-bar` -- compact header: "Venus OS Fronius Proxy" title + connection status dots, `background: var(--ve-bg-surface)`
    - `.page` -- `display: none` by default
    - `.page.active` -- `display: block`

    Venus OS widget styles:
    - `.ve-panel` -- `background: var(--ve-bg-surface)`, `border: 1px solid var(--ve-border)`, `border-radius: 12px`, `padding: 16px`
    - `.ve-card` -- similar to panel but with `background: var(--ve-bg-widget)` for highlighted items
    - `.ve-grid` -- `display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 12px`
    - Status dot: `.ve-dot` 12px circle, `.ve-dot--ok` green, `.ve-dot--warn` orange, `.ve-dot--err` red
    - Buttons: `.ve-btn`, `.ve-btn--primary` (blue), `.ve-btn--danger` (red)
    - Form inputs: `.ve-input` styled against dark background
    - Register viewer: `.ve-reg-header`, `.ve-reg-row` with grid columns matching current layout

    Responsive breakpoints:
    - Desktop (>1024px): sidebar expanded 220px
    - Tablet (768-1024px): `.app-shell { grid-template-columns: 56px 1fr }`, `.nav-label { display: none }`
    - Mobile (<768px): `.app-shell { grid-template-columns: 1fr }`, sidebar position fixed off-screen, `.sidebar.open { left: 0 }`, hamburger button visible

    **index.html** -- COMPLETE rewrite (not incremental edit) of `src/venus_os_fronius_proxy/static/index.html`:
    - `<link rel="stylesheet" href="/static/style.css">`
    - `<script src="/static/app.js" defer></script>`
    - NO inline `<style>` tags (all styles in style.css per anti-pattern rule)
    - NO inline `<script>` tags (all JS in app.js)
    - App shell structure:
      ```html
      <div class="app-shell">
        <nav class="sidebar" id="sidebar">
          <div class="sidebar-header"><!-- inline SVG logo --></div>
          <a class="nav-item active" data-page="dashboard">
            <svg class="nav-icon"><!-- gauge/dashboard icon --></svg>
            <span class="nav-label">Dashboard</span>
          </a>
          <a class="nav-item" data-page="config">
            <svg class="nav-icon"><!-- settings/gear icon --></svg>
            <span class="nav-label">Config</span>
          </a>
          <a class="nav-item" data-page="registers">
            <svg class="nav-icon"><!-- list/table icon --></svg>
            <span class="nav-label">Registers</span>
          </a>
        </nav>
        <main class="content">
          <header class="top-bar">
            <button class="hamburger" id="hamburger"><!-- 3 lines SVG --></button>
            <span class="top-bar-title">Venus OS Fronius Proxy</span>
            <div class="top-bar-status">
              <span class="ve-dot" id="se-dot"></span>
              <span class="ve-dot" id="vos-dot"></span>
            </div>
          </header>
          <div id="page-dashboard" class="page active">
            <!-- Phase 6 fills this. For now: placeholder with connection status + health panels -->
            <div class="ve-panel">
              <h2>Dashboard</h2>
              <p class="ve-text-dim">Live data coming in Phase 6. Connection status and health below.</p>
            </div>
            <div class="ve-panel" id="status-panel"><!-- status rows --></div>
            <div class="ve-panel" id="health-panel"><!-- health grid --></div>
          </div>
          <div id="page-config" class="page">
            <!-- Config form, same fields as v1.0 -->
          </div>
          <div id="page-registers" class="page">
            <!-- Register viewer container -->
            <div class="ve-panel">
              <h2>Register Viewer</h2>
              <div id="register-models"></div>
            </div>
          </div>
        </main>
      </div>
      ```
    - SVG icons: simple inline SVGs for dashboard (gauge icon), config (gear icon), registers (list icon), hamburger (3 horizontal lines). ~10-15 lines each, no external dependencies.
    - Title: "Venus OS Fronius Proxy"
    - Sidebar header: simple SVG solar panel silhouette + proxy name

    **tests/test_theme.py** -- Smoke tests:
    - `test_venus_colors`: Read style.css, assert contains `--ve-blue: #387DC5`, `--ve-bg: #141414`, `--ve-text: #FAF9F5`, `--ve-text-dim: #969591`
    - `test_html_references`: Read index.html, assert contains `href="/static/style.css"` and `src="/static/app.js"`
    - `test_no_inline_styles`: Read index.html, assert `<style>` tag does NOT appear
    - `test_no_inline_scripts`: Read index.html, assert no `<script>` tag without `src=` attribute
    - `test_sidebar_navigation`: Read index.html, assert contains `data-page="dashboard"`, `data-page="config"`, `data-page="registers"`
    - `test_responsive_breakpoints`: Read style.css, assert contains `@media` with `768px` and `1024px`
    - Tests read files via `importlib.resources.files("venus_os_fronius_proxy") / "static" / filename`
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/test_theme.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "#387DC5" src/venus_os_fronius_proxy/static/style.css` shows Venus OS blue
    - `grep "#141414" src/venus_os_fronius_proxy/static/style.css` shows Venus OS background
    - `grep "#FAF9F5" src/venus_os_fronius_proxy/static/style.css` shows Venus OS text color
    - `grep "#969591" src/venus_os_fronius_proxy/static/style.css` shows Venus OS dim text
    - `grep "static/style.css" src/venus_os_fronius_proxy/static/index.html` shows CSS link
    - `grep "static/app.js" src/venus_os_fronius_proxy/static/index.html` shows JS script tag
    - `grep -c "<style>" src/venus_os_fronius_proxy/static/index.html` returns 0 (no inline styles)
    - `grep "data-page" src/venus_os_fronius_proxy/static/index.html` shows 3 nav items
    - `grep "@media" src/venus_os_fronius_proxy/static/style.css` shows responsive breakpoints
    - `grep "app-shell" src/venus_os_fronius_proxy/static/style.css` shows grid layout
    - `grep "ve-panel" src/venus_os_fronius_proxy/static/style.css` shows widget style
    - `python -m pytest tests/test_theme.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    index.html is a clean shell with sidebar layout, no inline styles/scripts. style.css has full Venus OS palette and responsive breakpoints. Smoke tests verify colors and file references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create app.js with navigation, polling, config form, and register viewer</name>
  <files>
    src/venus_os_fronius_proxy/static/app.js
  </files>
  <read_first>
    src/venus_os_fronius_proxy/static/index.html,
    src/venus_os_fronius_proxy/static/style.css,
    .planning/phases/05-data-pipeline-theme-foundation/05-CONTEXT.md
  </read_first>
  <action>
    Create `src/venus_os_fronius_proxy/static/app.js` -- COMPLETE rewrite of existing inline JS into module structure.

    **Navigation system:**
    ```javascript
    // Page navigation via sidebar
    document.querySelectorAll('.nav-item').forEach(item => {
        item.addEventListener('click', (e) => {
            e.preventDefault();
            const page = item.dataset.page;
            // Hide all pages, show selected
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
            document.getElementById('page-' + page).classList.add('active');
            // Update nav active state
            document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
            item.classList.add('active');
            // Close mobile sidebar
            document.getElementById('sidebar').classList.remove('open');
        });
    });
    ```

    **Hamburger toggle** (mobile sidebar):
    ```javascript
    document.getElementById('hamburger').addEventListener('click', () => {
        document.getElementById('sidebar').classList.toggle('open');
    });
    ```

    **Polling functions** -- Port ALL existing functionality from v1.0 index.html:
    1. `pollStatus()` -- fetches `/api/status`, updates `#se-dot` and `#vos-dot` classes (use `ve-dot--ok`, `ve-dot--warn`, `ve-dot--err`), updates status text in the status panel
    2. `pollHealth()` -- fetches `/api/health`, updates uptime, poll rate, last poll age, cache status in the health panel
    3. `loadConfig()` -- fetches `/api/config`, populates form fields `#se-host`, `#se-port`, `#se-unit`
    4. Test connection button handler -- POST to `/api/config/test`, show result in `#config-message`
    5. Save config form handler -- POST to `/api/config`, show result in `#config-message`
    6. `pollRegisters()` -- fetches `/api/registers`, builds collapsible model groups with side-by-side SE30K/Fronius columns, change detection with flash animation
    7. `formatValue(val)` -- same null/string/number formatting

    **Register viewer** -- Same logic as v1.0 but using `.ve-` prefixed classes:
    - `.ve-model-group`, `.ve-model-header`, `.ve-model-fields`, `.ve-model-fields.collapsed`
    - `.ve-reg-header`, `.ve-reg-row` with grid columns: Addr | Name | SE30K Source | Fronius Target
    - Change flash animation using `.ve-changed` class

    **Poll interval:** `const POLL_INTERVAL = 2000;` (same as v1.0)

    **Initialization:**
    ```javascript
    document.addEventListener('DOMContentLoaded', () => {
        loadConfig();
        pollStatus();
        pollHealth();
        pollRegisters();
        setInterval(() => { pollStatus(); pollHealth(); pollRegisters(); }, POLL_INTERVAL);
    });
    ```

    **Key differences from v1.0:**
    - All CSS classes use `ve-` prefix
    - Status dots in top-bar header (compact, not a full section)
    - Config form and register viewer are in separate "pages" (divs) toggled by sidebar
    - No inline event handlers -- all via addEventListener
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pollStatus" src/venus_os_fronius_proxy/static/app.js` shows status polling function
    - `grep "pollHealth" src/venus_os_fronius_proxy/static/app.js` shows health polling function
    - `grep "pollRegisters" src/venus_os_fronius_proxy/static/app.js` shows register polling
    - `grep "loadConfig" src/venus_os_fronius_proxy/static/app.js` shows config loading
    - `grep "/api/status" src/venus_os_fronius_proxy/static/app.js` shows status endpoint fetch
    - `grep "/api/health" src/venus_os_fronius_proxy/static/app.js` shows health endpoint fetch
    - `grep "/api/registers" src/venus_os_fronius_proxy/static/app.js` shows register endpoint fetch
    - `grep "/api/config" src/venus_os_fronius_proxy/static/app.js` shows config endpoint fetch
    - `grep "data-page" src/venus_os_fronius_proxy/static/app.js` shows navigation wiring
    - `grep "hamburger" src/venus_os_fronius_proxy/static/app.js` shows mobile menu toggle
    - `grep "POLL_INTERVAL" src/venus_os_fronius_proxy/static/app.js` shows polling interval constant
    - `grep "setInterval" src/venus_os_fronius_proxy/static/app.js` shows polling loop
    - `python -m pytest tests/ -x -q` exits 0 (all tests still pass)
  </acceptance_criteria>
  <done>
    app.js contains all v1.0 functionality (status, health, config, register viewer) plus sidebar navigation and hamburger toggle. All existing tests pass. Frontend is fully functional with 3-file split.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Venus OS themed UI in browser</name>
  <files>
    src/venus_os_fronius_proxy/static/index.html,
    src/venus_os_fronius_proxy/static/style.css,
    src/venus_os_fronius_proxy/static/app.js
  </files>
  <action>
    Human visual verification of the complete frontend. No code changes in this task.

    Start the proxy and open the webapp in a browser. Verify:
    1. Dark background (#141414), blue accent (#387DC5) visible
    2. Sidebar on the left with 3 navigation items (Dashboard, Config, Registers) each with an icon
    3. Compact header with "Venus OS Fronius Proxy" title and connection status dots
    4. Click "Config" -- config form appears with IP/port/unit fields
    5. Click "Registers" -- register viewer with collapsible model groups, side-by-side columns
    6. Click "Dashboard" -- placeholder panel with status and health info
    7. Resize browser: tablet (~900px) shows icon-only sidebar, mobile (~500px) shows hamburger
    8. Click hamburger on mobile -- sidebar slides in as overlay
    9. Browser console has no JS errors or MIME type warnings
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    User confirms Venus OS themed UI renders correctly with working navigation, config form, register viewer, and responsive layout across desktop, tablet, and mobile widths.
  </done>
</task>

</tasks>

<verification>
```bash
# All tests pass
cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/ -v

# Theme smoke tests
python -m pytest tests/test_theme.py -v

# Static files exist and are importable
python -c "
import importlib.resources as r
pkg = r.files('venus_os_fronius_proxy') / 'static'
for f in ['index.html', 'style.css', 'app.js']:
    content = (pkg / f).read_text()
    print(f'{f}: {len(content)} chars')
"

# Verify no inline styles in HTML
python -c "
import importlib.resources as r
html = (r.files('venus_os_fronius_proxy') / 'static' / 'index.html').read_text()
assert '<style>' not in html, 'Found inline styles!'
print('No inline styles: OK')
"
```
</verification>

<success_criteria>
- index.html is a clean shell with no inline styles or scripts
- style.css contains exact Venus OS gui-v2 color palette as CSS custom properties
- Sidebar navigation switches between Dashboard, Config, and Registers pages
- All v1.0 functionality works: status dots, health metrics, config form (test + save), register viewer (collapsible, side-by-side, change flash)
- Responsive: expanded sidebar > icon-only > hamburger overlay
- Browser console shows no errors or MIME type warnings
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-pipeline-theme-foundation/05-02-SUMMARY.md`
</output>
