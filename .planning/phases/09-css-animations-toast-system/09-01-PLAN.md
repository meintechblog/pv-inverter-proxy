---
phase: 09-css-animations-toast-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/venus_os_fronius_proxy/static/style.css
  - src/venus_os_fronius_proxy/static/app.js
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03, ANIM-04]

must_haves:
  truths:
    - "Power gauge arc animates smoothly at 0.5s when values change, with no jitter on small fluctuations"
    - "Dashboard widgets appear with staggered entrance animation on first WebSocket connection"
    - "Value changes in cards produce a subtle highlight only on significant changes, not every 1Hz update"
    - "All animations are disabled when prefers-reduced-motion is active"
  artifacts:
    - path: "src/venus_os_fronius_proxy/static/style.css"
      provides: "Animation CSS variables, prefers-reduced-motion rule, entrance keyframes, gauge transition update"
      contains: "prefers-reduced-motion"
    - path: "src/venus_os_fronius_proxy/static/app.js"
      provides: "Gauge deadband, flash thresholds, entrance animation trigger"
      contains: "GAUGE_DEADBAND_W"
  key_links:
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "src/venus_os_fronius_proxy/static/style.css"
      via: "ve-card--entering class toggling triggers CSS animation"
      pattern: "ve-card--entering"
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "src/venus_os_fronius_proxy/static/style.css"
      via: "Gauge deadband guards stroke-dashoffset updates"
      pattern: "GAUGE_DEADBAND_W"
---

<objective>
Add CSS animation foundation and all animation behaviors to the dashboard: animation timing variables, gauge arc improvements (0.5s transition + deadband), threshold-based value flash, staggered entrance animations, and prefers-reduced-motion accessibility support.

Purpose: Establish the "Subtle Industrial" animation feel -- animations communicate state changes (SCADA-style), not decorate. GPU-accelerated only (transform, opacity). Foundation for toast system in Plan 02.
Output: Updated style.css with animation variables, keyframes, and reduced-motion rule. Updated app.js with gauge deadband, flash thresholds, and entrance animation trigger.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-css-animations-toast-system/09-CONTEXT.md
@.planning/phases/09-css-animations-toast-system/09-RESEARCH.md

<interfaces>
<!-- Current codebase state executor needs to understand -->

From style.css :root (lines 4-24):
```css
:root {
    --ve-blue: #387DC5;
    --ve-blue-light: #73A2D3;
    --ve-blue-dim: #27588A;
    --ve-orange: #F0962E;
    --ve-red: #F35C58;
    --ve-green: #72B84C;
    --ve-bg: #141414;
    --ve-bg-surface: #272622;
    --ve-bg-widget: #11263B;
    --ve-border: #64635F;
    --ve-text: #FAF9F5;
    --ve-text-dim: #969591;
    --ve-text-secondary: #DCDBD7;
    --ve-ok: var(--ve-blue);
    --ve-warning: var(--ve-orange);
    --ve-error: var(--ve-red);
    --ve-success: var(--ve-green);
    --ve-mono: 'Courier New', monospace;
    --ve-font: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}
```

From style.css gauge (line 616-618):
```css
#gauge-fill {
    transition: stroke-dashoffset 0.8s ease-out, stroke 0.5s ease;
}
```

From style.css flash animation (lines 444-452, 662-664):
```css
@keyframes ve-flash {
    0% { background-color: var(--ve-blue-dim); }
    100% { background-color: transparent; }
}
.ve-changed {
    animation: ve-flash 0.5s ease-out;
}
.ve-value-flash {
    color: var(--ve-blue-light) !important;
}
```

From app.js updateGauge (lines 144-162):
```javascript
function updateGauge(powerW) {
    var pct = Math.min(powerW / CAPACITY_W, 1.0);
    var arcLength = 251.3;
    var offset = arcLength * (1 - pct);
    var gaugeFill = document.getElementById('gauge-fill');
    var gaugeValue = document.getElementById('gauge-value');
    if (gaugeFill) {
        gaugeFill.style.strokeDashoffset = offset;
        var color = pct < 0.5 ? 'var(--ve-green)' : pct < 0.8 ? 'var(--ve-orange)' : 'var(--ve-red)';
        gaugeFill.style.stroke = color;
    }
    if (gaugeValue) {
        gaugeValue.textContent = (powerW / 1000).toFixed(1);
    }
}
```

From app.js flashValue (lines 271-276):
```javascript
function flashValue(el) {
    el.classList.add('ve-value-flash');
    setTimeout(function() {
        el.classList.remove('ve-value-flash');
    }, 300);
}
```

From app.js WebSocket onopen (lines 53-56):
```javascript
ws.onopen = function() {
    reconnectDelay = 1000;
    updateConnectionIndicator('connected');
};
```

Dashboard cards in index.html use class `ve-card` (lines 91, 116, 122, 128, 137, 148, 162, 177).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add animation CSS variables, gauge transition update, entrance keyframes, and prefers-reduced-motion</name>
  <files>src/venus_os_fronius_proxy/static/style.css</files>
  <read_first>
    - src/venus_os_fronius_proxy/static/style.css
    - .planning/phases/09-css-animations-toast-system/09-RESEARCH.md
  </read_first>
  <action>
Modify style.css with these specific changes:

1. **Add animation CSS custom properties** to the existing `:root` block (after line 23, before the closing `}`):
```css
    --ve-duration-fast: 150ms;
    --ve-duration-normal: 300ms;
    --ve-duration-slow: 500ms;
    --ve-easing-default: cubic-bezier(0.4, 0, 0.2, 1);
    --ve-easing-out: cubic-bezier(0, 0, 0.2, 1);
```

2. **Update gauge transition** on `#gauge-fill` (line 617) from `0.8s` to `0.5s` and add `will-change`:
```css
#gauge-fill {
    transition: stroke-dashoffset 0.5s ease-out, stroke 0.5s ease;
    will-change: stroke-dashoffset;
}
```

3. **Add entrance animation keyframe and classes** after the existing `ve-flash` block (after line 452):
```css
/* ===== Entrance Animation ===== */
@keyframes ve-slide-up {
    from { opacity: 0; transform: translateY(12px); }
    to   { opacity: 1; transform: translateY(0); }
}

.ve-card--entering {
    animation: ve-slide-up var(--ve-duration-normal) var(--ve-easing-out) both;
}
.ve-card--entering:nth-child(1) { animation-delay: 0ms; }
.ve-card--entering:nth-child(2) { animation-delay: 60ms; }
.ve-card--entering:nth-child(3) { animation-delay: 120ms; }
.ve-card--entering:nth-child(4) { animation-delay: 180ms; }
.ve-card--entering:nth-child(5) { animation-delay: 240ms; }
.ve-card--entering:nth-child(6) { animation-delay: 300ms; }
.ve-card--entering:nth-child(7) { animation-delay: 360ms; }
.ve-card--entering:nth-child(8) { animation-delay: 420ms; }
```

4. **Add prefers-reduced-motion rule** as the LAST rule in style.css (after all other CSS, at the very end of file):
```css
/* ===== Accessibility: Reduced Motion ===== */
@media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
    }
}
```

IMPORTANT: The prefers-reduced-motion rule MUST be the very last rule in the file to override everything. Do NOT place it before the responsive media queries or toast styles.
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && grep -c "ve-duration-fast" src/venus_os_fronius_proxy/static/style.css && grep -c "ve-slide-up" src/venus_os_fronius_proxy/static/style.css && grep -c "prefers-reduced-motion" src/venus_os_fronius_proxy/static/style.css && grep "stroke-dashoffset 0.5s" src/venus_os_fronius_proxy/static/style.css && grep "will-change: stroke-dashoffset" src/venus_os_fronius_proxy/static/style.css && tail -5 src/venus_os_fronius_proxy/static/style.css | grep "prefers-reduced-motion\|important"</automated>
  </verify>
  <acceptance_criteria>
    - grep "ve-duration-fast: 150ms" style.css returns match
    - grep "ve-duration-normal: 300ms" style.css returns match
    - grep "ve-duration-slow: 500ms" style.css returns match
    - grep "ve-easing-default" style.css returns match
    - grep "ve-easing-out" style.css returns match
    - grep "stroke-dashoffset 0.5s" style.css returns match (was 0.8s)
    - grep "will-change: stroke-dashoffset" style.css returns match
    - grep "ve-slide-up" style.css returns match
    - grep "ve-card--entering" style.css returns at least 9 matches (class + 8 nth-child)
    - grep "prefers-reduced-motion" style.css returns at least 1 match
    - The last CSS rule block in the file contains "prefers-reduced-motion"
    - No animation uses width, height, top, left, or margin properties (GPU-accelerated only)
  </acceptance_criteria>
  <done>style.css has animation timing variables in :root, gauge transition at 0.5s with will-change, ve-slide-up entrance keyframe with stagger classes, and prefers-reduced-motion as the final rule</done>
</task>

<task type="auto">
  <name>Task 2: Add gauge deadband, flash thresholds, and entrance animation trigger to app.js</name>
  <files>src/venus_os_fronius_proxy/static/app.js</files>
  <read_first>
    - src/venus_os_fronius_proxy/static/app.js
    - .planning/phases/09-css-animations-toast-system/09-RESEARCH.md
  </read_first>
  <action>
Modify app.js with these specific changes:

1. **Add gauge deadband** — Replace the `updateGauge` function (lines 144-162) with a version that skips updates below threshold. Add a `lastGaugePower` variable before the function:
```javascript
var lastGaugePower = -1;
var GAUGE_DEADBAND_W = 50;

function updateGauge(powerW) {
    if (lastGaugePower >= 0 && Math.abs(powerW - lastGaugePower) < GAUGE_DEADBAND_W) return;
    lastGaugePower = powerW;
    var pct = Math.min(powerW / CAPACITY_W, 1.0);
    var arcLength = 251.3;
    var offset = arcLength * (1 - pct);

    var gaugeFill = document.getElementById('gauge-fill');
    var gaugeValue = document.getElementById('gauge-value');

    if (gaugeFill) {
        gaugeFill.style.strokeDashoffset = offset;
        var color = pct < 0.5 ? 'var(--ve-green)' : pct < 0.8 ? 'var(--ve-orange)' : 'var(--ve-red)';
        gaugeFill.style.stroke = color;
    }

    if (gaugeValue) {
        gaugeValue.textContent = (powerW / 1000).toFixed(1);
    }
}
```
Note: `lastGaugePower` starts at -1 so the first update always applies.

2. **Add threshold-based flash** — Replace the `flashValue` function (lines 271-276) with a version that checks numeric thresholds. Add threshold map and previous-value tracking:
```javascript
var FLASH_THRESHOLDS = {
    'voltage': 2,       // > 2V change
    'current': 0.5,     // > 0.5A change
    'power': 100,       // > 100W change
    'temperature': 1,   // > 1C change
    'default': 0        // always flash for unknown types
};
var lastFlashValues = {};

function flashValue(el, metricType) {
    var numericValue = parseFloat(el.textContent);
    if (metricType && !isNaN(numericValue)) {
        var threshold = FLASH_THRESHOLDS[metricType] || FLASH_THRESHOLDS['default'];
        var key = el.id || el.textContent;
        if (lastFlashValues[key] !== undefined && Math.abs(numericValue - lastFlashValues[key]) < threshold) {
            lastFlashValues[key] = numericValue;
            return;
        }
        lastFlashValues[key] = numericValue;
    }
    el.classList.add('ve-value-flash');
    setTimeout(function() {
        el.classList.remove('ve-value-flash');
    }, 300);
}
```

3. **Update all flashValue call sites** to pass the metric type as second argument. Find and update these specific calls:
   - Line 199: `flashValue(tempCab)` -> `flashValue(tempCab, 'temperature')`
   - Line 203: `flashValue(tempSink)` -> `flashValue(tempSink, 'temperature')`
   - Line 209: `flashValue(dcV)` -> `flashValue(dcV, 'voltage')`
   - Line 213: `flashValue(dcA)` -> `flashValue(dcA, 'current')`
   - Line 217: `flashValue(dcW)` -> `flashValue(dcW, 'power')`
   - Line 230: `flashValue(el)` -> keep as `flashValue(el)` (status text, always flash)
   - Line 245: `flashValue(voltageEl)` -> `flashValue(voltageEl, 'voltage')`
   - Line 253: `flashValue(currentEl)` -> `flashValue(currentEl, 'current')`
   - Line 266: `flashValue(powerEl)` -> `flashValue(powerEl, 'power')`

4. **Add one-shot entrance animation trigger** in `ws.onopen` (line 53). Add a flag variable before `connectWebSocket()`:
```javascript
var entranceAnimated = false;
```
Inside `ws.onopen`, after `updateConnectionIndicator('connected');` add:
```javascript
        if (!entranceAnimated) {
            var cards = document.querySelectorAll('#page-dashboard .ve-card');
            for (var i = 0; i < cards.length; i++) {
                cards[i].classList.add('ve-card--entering');
            }
            entranceAnimated = true;
            setTimeout(function() {
                var entering = document.querySelectorAll('.ve-card--entering');
                for (var j = 0; j < entering.length; j++) {
                    entering[j].classList.remove('ve-card--entering');
                }
            }, 800);
        }
```
This fires once on initial WebSocket connection, adds the CSS class for staggered entrance, then removes it after all animations complete (420ms max delay + 300ms duration = 720ms, use 800ms for safety). Subsequent reconnects do NOT replay the animation.

5. **Add reduced-motion JS guard** at the top of app.js (after the initial variable declarations):
```javascript
var prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
```
Then guard the entrance animation: wrap the `if (!entranceAnimated)` block with `if (!prefersReducedMotion.matches)`:
```javascript
        if (!entranceAnimated && !prefersReducedMotion.matches) {
```
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && grep -c "GAUGE_DEADBAND_W" src/venus_os_fronius_proxy/static/app.js && grep -c "FLASH_THRESHOLDS" src/venus_os_fronius_proxy/static/app.js && grep -c "entranceAnimated" src/venus_os_fronius_proxy/static/app.js && grep -c "prefersReducedMotion" src/venus_os_fronius_proxy/static/app.js && grep "flashValue(tempCab, 'temperature')" src/venus_os_fronius_proxy/static/app.js && grep "flashValue(dcV, 'voltage')" src/venus_os_fronius_proxy/static/app.js && grep "ve-card--entering" src/venus_os_fronius_proxy/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "GAUGE_DEADBAND_W = 50" app.js returns match
    - grep "lastGaugePower" app.js returns at least 3 matches (declaration, check, assignment)
    - grep "FLASH_THRESHOLDS" app.js returns at least 1 match
    - grep "flashValue(tempCab, 'temperature')" app.js returns match
    - grep "flashValue(tempSink, 'temperature')" app.js returns match
    - grep "flashValue(dcV, 'voltage')" app.js returns match
    - grep "flashValue(dcA, 'current')" app.js returns match
    - grep "flashValue(dcW, 'power')" app.js returns match
    - grep "flashValue(voltageEl, 'voltage')" app.js returns match
    - grep "flashValue(currentEl, 'current')" app.js returns match
    - grep "flashValue(powerEl, 'power')" app.js returns match
    - grep "entranceAnimated" app.js returns at least 3 matches
    - grep "ve-card--entering" app.js returns at least 2 matches
    - grep "prefersReducedMotion" app.js returns at least 2 matches
    - python -m pytest tests/ -x -q passes (no backend regression)
  </acceptance_criteria>
  <done>app.js has 50W gauge deadband preventing jitter, threshold-based flashValue with per-metric thresholds, one-shot entrance animation on first WS connection, and prefers-reduced-motion JS guard</done>
</task>

</tasks>

<verification>
1. `grep "ve-duration-fast" src/venus_os_fronius_proxy/static/style.css` confirms animation variables exist
2. `grep "stroke-dashoffset 0.5s" src/venus_os_fronius_proxy/static/style.css` confirms gauge transition updated
3. `grep "prefers-reduced-motion" src/venus_os_fronius_proxy/static/style.css` confirms accessibility rule
4. `grep "GAUGE_DEADBAND_W" src/venus_os_fronius_proxy/static/app.js` confirms deadband logic
5. `grep "FLASH_THRESHOLDS" src/venus_os_fronius_proxy/static/app.js` confirms threshold-based flash
6. `grep "entranceAnimated" src/venus_os_fronius_proxy/static/app.js` confirms one-shot entrance
7. `python -m pytest tests/ -x -q` passes (no backend regression)
</verification>

<success_criteria>
- Gauge arc transition is 0.5s with 50W deadband (ANIM-01)
- Dashboard cards have ve-card--entering with staggered delays (ANIM-02)
- flashValue uses per-metric thresholds to suppress trivial changes (ANIM-03)
- prefers-reduced-motion CSS rule and JS matchMedia guard both present (ANIM-04)
- All animations use only transform and opacity (GPU-accelerated)
</success_criteria>

<output>
After completion, create `.planning/phases/09-css-animations-toast-system/09-01-SUMMARY.md`
</output>
