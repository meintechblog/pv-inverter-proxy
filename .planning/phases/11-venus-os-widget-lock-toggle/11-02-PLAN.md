---
phase: 11-venus-os-widget-lock-toggle
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/venus_os_fronius_proxy/static/index.html
  - src/venus_os_fronius_proxy/static/style.css
  - src/venus_os_fronius_proxy/static/app.js
autonomous: false
requirements: [VENUS-01, VENUS-02, VENUS-03, VENUS-04]

must_haves:
  truths:
    - "Dashboard shows a Venus OS widget card with connection status (Online/Offline)"
    - "Widget shows last contact timestamp as relative time"
    - "Widget shows current override value when Venus OS is active"
    - "Apple-style toggle allows locking Venus OS control"
    - "Confirmation dialog appears before locking, with auto-unlock time shown"
    - "Countdown timer is visible while locked, interpolated smoothly between snapshots"
    - "Toast notification on lock/unlock and auto-unlock"
    - "Lock toggle is disabled when Venus OS is not active (no writes in last 120s)"
  artifacts:
    - path: "src/venus_os_fronius_proxy/static/index.html"
      provides: "Venus OS widget card HTML with lock toggle, countdown, status display"
      contains: "venus-os-panel"
    - path: "src/venus_os_fronius_proxy/static/style.css"
      provides: "Apple-style toggle CSS, venus card styles, lock countdown styles"
      contains: "ve-toggle"
    - path: "src/venus_os_fronius_proxy/static/app.js"
      provides: "updateVenusInfo handler, sendLockCommand, countdown interpolation"
      contains: "sendLockCommand"
  key_links:
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "/api/venus-lock"
      via: "fetch POST in sendLockCommand"
      pattern: "venus-lock"
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "snapshot.venus_os"
      via: "handleSnapshot reads venus_os section"
      pattern: "venus_os"
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "showConfirmDialog"
      via: "Lock confirmation reuses existing dialog"
      pattern: "showConfirmDialog"
---

<objective>
Add Venus OS info widget to the dashboard with connection status, override display, Apple-style lock toggle with confirmation dialog, and countdown timer. Wire to backend /api/venus-lock endpoint and snapshot venus_os data.

Purpose: Give users visibility into Venus OS connection state and the ability to temporarily lock Venus OS power control with safety guarantees.
Output: Complete frontend widget with lock toggle, visible in dashboard.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-venus-os-widget-lock-toggle/11-CONTEXT.md
@.planning/phases/11-venus-os-widget-lock-toggle/11-RESEARCH.md
@.planning/phases/11-venus-os-widget-lock-toggle/11-01-SUMMARY.md

<interfaces>
<!-- Backend contracts from Plan 01 -->

Snapshot venus_os section (from dashboard.py):
```python
venus_os = {
    "last_source": "none" | "venus_os" | "webapp",   # Who last wrote
    "last_change_ts": 1710790800.0,                   # time.time() of last write
    "is_locked": False,                                # Lock state
    "lock_remaining_s": None | 892.5,                  # Seconds until auto-unlock
}
```

POST /api/venus-lock endpoint (from webapp.py):
```
POST /api/venus-lock
Body: {"action": "lock"} or {"action": "unlock"}
Response: {"success": true} or {"success": false, "error": "..."}
```

Existing JS functions available:
```javascript
function showConfirmDialog(message, onConfirm) { ... }  // Confirmation modal
function showToast(message, type) { ... }                // type: info|success|warning|error
```

CSS variables available (from Phase 9):
```css
--ve-duration-normal: 0.3s;
--ve-easing-default: cubic-bezier(0.4, 0, 0.2, 1);
--ve-green: #4caf50;
--ve-red: #f44336;
--ve-border: ...;
--ve-text: ...;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Venus OS widget HTML, Apple-style toggle CSS, and JavaScript handlers</name>
  <files>
    src/venus_os_fronius_proxy/static/index.html,
    src/venus_os_fronius_proxy/static/style.css,
    src/venus_os_fronius_proxy/static/app.js
  </files>
  <read_first>
    src/venus_os_fronius_proxy/static/index.html,
    src/venus_os_fronius_proxy/static/style.css,
    src/venus_os_fronius_proxy/static/app.js
  </read_first>
  <action>
    **index.html changes:**
    Add a new ve-card inside the `.ve-dashboard-bottom` grid, AFTER the `peak-stats-panel` card:

    ```html
    <div class="ve-card" id="venus-os-panel">
      <h2 class="ve-card-title">Venus OS Control</h2>
      <div class="ve-venus-status-row">
        <span class="ve-status-dot" id="venus-status-dot"></span>
        <span id="venus-status-text">--</span>
      </div>
      <div class="ve-kv-grid">
        <div><label>Override</label><span class="ve-live-value" id="venus-override">--</span></div>
        <div><label>Last Contact</label><span class="ve-live-value" id="venus-last-contact">--</span></div>
      </div>
      <div class="ve-venus-lock" id="venus-lock-section">
        <div class="ve-venus-lock-row">
          <span class="ve-venus-lock-label">Lock Venus OS</span>
          <label class="ve-toggle" id="venus-lock-container">
            <input type="checkbox" id="venus-lock-toggle" disabled>
            <span class="ve-toggle-track"></span>
          </label>
        </div>
        <div class="ve-lock-countdown" id="lock-countdown" style="display:none">
          Auto-unlock in: <span id="lock-countdown-time">15:00</span>
        </div>
      </div>
    </div>
    ```

    **style.css changes:**
    Add Apple-style toggle and Venus OS widget styles. Place AFTER existing card styles:

    ```css
    /* --- Apple-style Toggle --- */
    .ve-toggle {
        position: relative;
        display: inline-block;
        width: 52px;
        height: 28px;
    }
    .ve-toggle input {
        opacity: 0;
        width: 0;
        height: 0;
        position: absolute;
    }
    .ve-toggle-track {
        position: absolute;
        cursor: pointer;
        top: 0; left: 0; right: 0; bottom: 0;
        background: var(--ve-border);
        border-radius: 28px;
        transition: background var(--ve-duration-normal) var(--ve-easing-default);
    }
    .ve-toggle-track::before {
        content: '';
        position: absolute;
        height: 22px;
        width: 22px;
        left: 3px;
        bottom: 3px;
        background: var(--ve-text);
        border-radius: 50%;
        transition: transform var(--ve-duration-normal) cubic-bezier(0.34, 1.56, 0.64, 1);
    }
    .ve-toggle input:checked + .ve-toggle-track {
        background: var(--ve-red);
    }
    .ve-toggle input:checked + .ve-toggle-track::before {
        transform: translateX(24px);
    }
    .ve-toggle input:disabled + .ve-toggle-track {
        opacity: 0.4;
        cursor: not-allowed;
    }

    /* --- Venus OS Widget --- */
    .ve-venus-status-row {
        display: flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 12px;
    }
    .ve-status-dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        background: var(--ve-text-muted);
        flex-shrink: 0;
    }
    .ve-status-dot.online { background: var(--ve-green); }
    .ve-status-dot.offline { background: var(--ve-text-muted); }
    .ve-venus-lock {
        margin-top: 16px;
        padding-top: 12px;
        border-top: 1px solid var(--ve-border);
    }
    .ve-venus-lock-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
    }
    .ve-venus-lock-label {
        font-weight: 500;
    }
    .ve-lock-countdown {
        margin-top: 8px;
        font-size: 0.85em;
        color: var(--ve-red);
        font-variant-numeric: tabular-nums;
    }
    ```

    **app.js changes:**

    1. Add venus lock state tracking variables near the top (after `var TEMP_WARNING_C`):
    ```javascript
    var venusLockRemaining = null;
    var venusLockSnapshotTs = null;
    var venusCountdownInterval = null;
    ```

    2. Add `updateVenusInfo(snapshot)` function. Called from handleSnapshot:
    ```javascript
    function updateVenusInfo(snapshot) {
        var venus = snapshot.venus_os;
        if (!venus) return;

        var dot = document.getElementById('venus-status-dot');
        var statusText = document.getElementById('venus-status-text');
        var overrideEl = document.getElementById('venus-override');
        var lastContactEl = document.getElementById('venus-last-contact');
        var toggle = document.getElementById('venus-lock-toggle');
        var countdownDiv = document.getElementById('lock-countdown');
        var countdownTime = document.getElementById('lock-countdown-time');

        // Venus OS connection status: "Online" if last_source=="venus_os" AND last_change_ts within 120s
        var isOnline = false;
        if (venus.last_source === 'venus_os' && venus.last_change_ts > 0) {
            var age = (snapshot.ts - venus.last_change_ts);
            isOnline = age < 120;
        }

        dot.className = 've-status-dot ' + (isOnline ? 'online' : 'offline');
        statusText.textContent = isOnline ? 'Online' : 'Offline';

        // Override status
        var ctrl = snapshot.control;
        if (isOnline && ctrl && ctrl.enabled && ctrl.last_source === 'venus_os') {
            overrideEl.textContent = ctrl.limit_pct.toFixed(1) + '%';
        } else if (isOnline) {
            overrideEl.textContent = 'No override';
        } else {
            overrideEl.textContent = '--';
        }

        // Last contact (relative time)
        if (venus.last_change_ts > 0) {
            var ageSec = Math.floor(snapshot.ts - venus.last_change_ts);
            if (ageSec < 60) lastContactEl.textContent = ageSec + 's ago';
            else if (ageSec < 3600) lastContactEl.textContent = Math.floor(ageSec / 60) + 'm ago';
            else lastContactEl.textContent = Math.floor(ageSec / 3600) + 'h ago';
        } else {
            lastContactEl.textContent = 'Never';
        }

        // Lock toggle state
        toggle.checked = venus.is_locked;
        toggle.disabled = !isOnline && !venus.is_locked;  // Allow unlock even if offline

        // Countdown
        if (venus.is_locked && venus.lock_remaining_s != null) {
            countdownDiv.style.display = '';
            venusLockRemaining = venus.lock_remaining_s;
            venusLockSnapshotTs = Date.now() / 1000;
            updateCountdownDisplay();
            startCountdownInterval();
        } else {
            countdownDiv.style.display = 'none';
            venusLockRemaining = null;
            stopCountdownInterval();
        }
    }
    ```

    3. Add countdown interpolation functions:
    ```javascript
    function updateCountdownDisplay() {
        var el = document.getElementById('lock-countdown-time');
        if (!el || venusLockRemaining == null) return;
        var elapsed = Date.now() / 1000 - venusLockSnapshotTs;
        var remaining = Math.max(0, venusLockRemaining - elapsed);
        var min = Math.floor(remaining / 60);
        var sec = Math.floor(remaining % 60);
        el.textContent = min + ':' + (sec < 10 ? '0' : '') + sec;
    }

    function startCountdownInterval() {
        if (venusCountdownInterval) return;
        venusCountdownInterval = setInterval(updateCountdownDisplay, 1000);
    }

    function stopCountdownInterval() {
        if (venusCountdownInterval) {
            clearInterval(venusCountdownInterval);
            venusCountdownInterval = null;
        }
    }
    ```

    4. Add lock toggle event handler (after the countdown functions):
    ```javascript
    (function() {
        var toggle = document.getElementById('venus-lock-toggle');
        if (!toggle) return;
        toggle.addEventListener('change', function(e) {
            e.preventDefault();
            var wantLock = toggle.checked;
            if (wantLock) {
                toggle.checked = false;  // Revert until confirmed
                var unlockTime = new Date(Date.now() + 15 * 60 * 1000);
                var timeStr = unlockTime.getHours() + ':' + (unlockTime.getMinutes() < 10 ? '0' : '') + unlockTime.getMinutes();
                showConfirmDialog(
                    'Lock Venus OS control for <strong>15 minutes</strong>?<br>' +
                    'Venus OS will not be able to limit inverter power during this time.<br>' +
                    'Auto-unlock at ' + timeStr + '.',
                    function() { sendLockCommand(true); }
                );
            } else {
                sendLockCommand(false);
            }
        });
    })();

    async function sendLockCommand(lock) {
        try {
            var res = await fetch('/api/venus-lock', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ action: lock ? 'lock' : 'unlock' })
            });
            var data = await res.json();
            if (data.success) {
                showToast(
                    lock ? 'Venus OS control locked (15 min)' : 'Venus OS control unlocked',
                    lock ? 'warning' : 'success'
                );
            } else {
                showToast(data.error || 'Failed to change lock state', 'error');
            }
        } catch (e) {
            showToast('Request failed: ' + e.message, 'error');
        }
    }
    ```

    5. In the existing `handleSnapshot(snapshot)` function (inside the ws.onmessage handler where snapshot data is processed), add a call to `updateVenusInfo(snapshot)` at the end of the snapshot handling block.

    6. Add auto-unlock toast detection in the snapshot diff section (where existing event detection happens for Venus OS override, fault, night mode):
    ```javascript
    // Detect auto-unlock event: was locked, now unlocked
    if (previousSnapshot && previousSnapshot.venus_os && snapshot.venus_os) {
        if (previousSnapshot.venus_os.is_locked && !snapshot.venus_os.is_locked) {
            showToast('Venus OS control auto-unlocked', 'info');
        }
    }
    ```

    IMPORTANT: The `previousSnapshot` variable is already used in app.js for event detection. The auto-unlock toast detection must go in the same block.
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && grep -q "venus-os-panel" src/venus_os_fronius_proxy/static/index.html && grep -q "ve-toggle" src/venus_os_fronius_proxy/static/style.css && grep -q "sendLockCommand" src/venus_os_fronius_proxy/static/app.js && grep -q "updateVenusInfo" src/venus_os_fronius_proxy/static/app.js && grep -q "venus-lock" src/venus_os_fronius_proxy/static/app.js && echo "ALL FRONTEND CHECKS PASS"</automated>
  </verify>
  <done>
    - Venus OS widget card visible in dashboard with status dot, override display, last contact
    - Apple-style toggle with red track when locked, disabled when Venus OS offline
    - Confirmation dialog before locking (shows auto-unlock time)
    - Countdown timer visible while locked, smoothly interpolated between snapshots
    - sendLockCommand calls POST /api/venus-lock
    - Auto-unlock toast fires when lock expires
    - updateVenusInfo called from handleSnapshot
  </done>
  <acceptance_criteria>
    - grep -q "venus-os-panel" src/venus_os_fronius_proxy/static/index.html
    - grep -q "ve-toggle-track" src/venus_os_fronius_proxy/static/style.css
    - grep -q "ve-toggle input:checked" src/venus_os_fronius_proxy/static/style.css
    - grep -q "sendLockCommand" src/venus_os_fronius_proxy/static/app.js
    - grep -q "updateVenusInfo" src/venus_os_fronius_proxy/static/app.js
    - grep -q "showConfirmDialog" src/venus_os_fronius_proxy/static/app.js
    - grep -q "lock-countdown" src/venus_os_fronius_proxy/static/index.html
    - grep -q "venus-lock-toggle" src/venus_os_fronius_proxy/static/index.html
    - grep -q "auto-unlocked" src/venus_os_fronius_proxy/static/app.js
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Venus OS widget and lock toggle visually</name>
  <files>src/venus_os_fronius_proxy/static/index.html</files>
  <action>
    Human visual verification of the Venus OS Control widget in the dashboard. No code changes -- this is a visual/functional check of Task 1 output.
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Human approves Venus OS widget appearance and lock toggle functionality</done>
  <what-built>
    Venus OS Control widget in dashboard with:
    - Connection status dot (green online / grey offline)
    - Override value display and last contact timestamp
    - Apple-style lock toggle (red when locked, smooth spring animation)
    - Confirmation dialog before locking
    - Countdown timer while locked (mm:ss format, counting down)
    - Toast notifications on lock/unlock/auto-unlock
  </what-built>
  <how-to-verify>
    1. Start the proxy: `cd "/Users/hulki/codex/venus os fronius proxy" && python -m venus_os_fronius_proxy`
    2. Open http://localhost:8080 in browser
    3. Look for "Venus OS Control" card in the bottom grid of dashboard
    4. Verify the card shows: status dot, status text (Online/Offline), Override field, Last Contact field
    5. Verify the lock toggle is present with label "Lock Venus OS"
    6. If Venus OS is not actively writing, toggle should appear disabled (greyed out)
    7. If Venus OS IS active: click the toggle ON -> confirm dialog should appear with 15-min warning and auto-unlock time
    8. Click Confirm -> toggle turns red, countdown appears showing mm:ss
    9. Click toggle OFF -> immediate unlock, toast "Venus OS control unlocked"
    10. Verify countdown counts down smoothly (no jumps between seconds)
    11. Check that the toggle knob slides with a spring-like animation (slight overshoot)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
  <acceptance_criteria>
    - Human confirms Venus OS widget is visible in dashboard
    - Human confirms lock toggle works with confirmation dialog
    - Human confirms countdown timer displays correctly
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd "/Users/hulki/codex/venus os fronius proxy"
# Full test suite
python -m pytest tests/ -x -q
# Frontend file checks
grep -q "venus-os-panel" src/venus_os_fronius_proxy/static/index.html
grep -q "ve-toggle" src/venus_os_fronius_proxy/static/style.css
grep -q "sendLockCommand" src/venus_os_fronius_proxy/static/app.js
grep -q "venus_lock_handler" src/venus_os_fronius_proxy/webapp.py
```
</verification>

<success_criteria>
- Venus OS widget card visible in dashboard bottom grid
- Connection status derived from last_change_ts recency (120s threshold)
- Override status shows current Venus OS power limit value
- Apple-style toggle with spring animation and red locked state
- Confirmation dialog before locking with auto-unlock time display
- Countdown timer smoothly interpolates between snapshot updates
- Lock toggle disabled when Venus OS is offline
- Toast on lock, unlock, and auto-unlock events
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/11-venus-os-widget-lock-toggle/11-02-SUMMARY.md`
</output>
