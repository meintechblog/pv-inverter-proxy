---
phase: 12-unified-dashboard-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/venus_os_fronius_proxy/static/index.html
  - src/venus_os_fronius_proxy/static/style.css
  - src/venus_os_fronius_proxy/static/app.js
autonomous: false
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03

must_haves:
  truths:
    - "Power control (slider, toggle, override log) appears inline below the power gauge on the dashboard page"
    - "All widgets are visible on the dashboard page in a compact grid without scrolling to another page"
    - "Sidebar shows only Dashboard, Config, and Registers — no Power Control nav item"
    - "Override log is collapsed by default with a toggle button showing event count"
    - "All power control JS functionality works (slider, apply, toggle, override log updates)"
  artifacts:
    - path: "src/venus_os_fronius_proxy/static/index.html"
      provides: "Unified single-page dashboard layout with inline power control"
      contains: "ctrl-slider"
    - path: "src/venus_os_fronius_proxy/static/style.css"
      provides: "2-row bottom grid and compact power control styles"
      contains: "ve-dashboard-row1"
    - path: "src/venus_os_fronius_proxy/static/app.js"
      provides: "Override log toggle, cleaned navigation"
      contains: "override-log-toggle"
  key_links:
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "index.html ctrl-* elements"
      via: "getElementById calls in IIFEs"
      pattern: "getElementById.*ctrl-"
    - from: "src/venus_os_fronius_proxy/static/app.js"
      to: "override-log-toggle button"
      via: "click event listener"
      pattern: "override-log-toggle"
---

<objective>
Merge Power Control inline into the Dashboard page, reorganize the bottom grid into 2 explicit rows, and remove the Power Control sidebar nav item. This completes the v2.1 milestone by unifying all widgets on a single page.

Purpose: Users no longer navigate between pages to control power — everything is on one dashboard.
Output: Restructured index.html, updated style.css with 2-row grid, cleaned app.js.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-unified-dashboard-layout/12-CONTEXT.md

<interfaces>
<!-- Current HTML structure the executor needs to understand -->

From index.html — Power Control elements to MOVE (lines 255-303):
- #ctrl-override-banner (Venus OS override warning)
- #ctrl-status with #ctrl-dot + #ctrl-label
- .ve-ctrl-readout with #ctrl-limit, #ctrl-source, #ctrl-ts
- #ctrl-slider-group with #ctrl-slider, #ctrl-slider-value, #ctrl-apply, #ctrl-toggle
- #ctrl-revert with #ctrl-revert-time
- #override-log

From index.html — Nav item to REMOVE (line 59-64):
```html
<a class="nav-item" data-page="power">...</a>
```

From index.html — Dashboard bottom grid (lines 147-217):
Currently uses .ve-dashboard-bottom with auto-fit grid containing:
- #inverter-status-panel, #status-panel, #health-panel, #peak-stats-panel, #venus-os-panel

From app.js — Power control IIFEs bind by getElementById:
- Slider Preview IIFE (line 863): ctrl-slider, ctrl-slider-value
- Apply Button IIFE (line 904): ctrl-apply, ctrl-slider
- Enable/Disable IIFE (line 922): ctrl-toggle
- updatePowerControl (line 957): ctrl-dot, ctrl-label, ctrl-limit, ctrl-source, ctrl-ts, ctrl-slider, ctrl-slider-value, ctrl-apply, ctrl-toggle, ctrl-revert, ctrl-revert-time, ctrl-override-banner
- updateOverrideLog (line 1064): override-log

ALL of these use getElementById — moving elements preserves bindings as long as IDs stay the same and elements exist before DOMContentLoaded (they will, since they move into #page-dashboard which loads before the deferred script).

From app.js — Navigation (line 17-32):
```javascript
document.querySelectorAll('.nav-item').forEach(item => {
    item.addEventListener('click', (e) => {
        const page = item.dataset.page;
        document.getElementById('page-' + page).classList.add('active');
    });
});
```
After removing data-page="power" nav item and #page-power section, clicking the old nav item is impossible. But guard getElementById('page-' + page) against null just in case.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure HTML and CSS for unified dashboard layout</name>
  <files>src/venus_os_fronius_proxy/static/index.html, src/venus_os_fronius_proxy/static/style.css</files>
  <read_first>
    - src/venus_os_fronius_proxy/static/index.html (full file — understand current #page-dashboard and #page-power structure)
    - src/venus_os_fronius_proxy/static/style.css (full file — understand .ve-dashboard-bottom, .ve-ctrl-* styles)
    - .planning/phases/12-unified-dashboard-layout/12-CONTEXT.md (user decisions)
  </read_first>
  <action>
    **index.html changes:**

    1. **Remove Power Control nav item** — Delete the `<a class="nav-item" data-page="power">` block (lines 59-64). Only Dashboard, Config, Registers remain.

    2. **Add inline power control section below the gauge card** — After the closing `</div>` of `.ve-dashboard-grid` (line 134) and before the sparkline card (line 137), insert a new compact power control section:
       ```html
       <!-- Inline Power Control -->
       <div class="ve-card ve-power-control-inline" id="power-control-inline">
         <div class="ve-power-inline-header">
           <h2 class="ve-card-title">Power Control <span class="ve-badge ve-badge--test">Manual Test</span></h2>
         </div>

         <!-- Venus OS Override Banner -->
         <div id="ctrl-override-banner" class="ve-ctrl-override-banner" style="display:none">
           Venus OS has control -- manual slider disabled
         </div>

         <!-- Status indicator -->
         <div id="ctrl-status" class="ve-ctrl-status">
           <span class="ve-dot" id="ctrl-dot"></span>
           <span id="ctrl-label">No limit active</span>
         </div>

         <!-- Read-only display -->
         <div class="ve-ctrl-readout">
           <div><label>Current Limit</label><span id="ctrl-limit">--</span></div>
           <div><label>Source</label><span id="ctrl-source">--</span></div>
           <div><label>Last Changed</label><span id="ctrl-ts">--</span></div>
         </div>

         <!-- Slider + buttons on one compact row -->
         <div class="ve-ctrl-slider-group" id="ctrl-slider-group">
           <input type="range" min="0" max="100" value="100" id="ctrl-slider" class="ve-ctrl-slider">
           <div class="ve-ctrl-slider-labels">
             <span>0%</span>
             <span id="ctrl-slider-value">100% = 30.0 kW</span>
             <span>100%</span>
           </div>
           <div class="ve-button-group">
             <button id="ctrl-apply" class="ve-btn ve-btn--primary">Apply</button>
             <button id="ctrl-toggle" class="ve-btn">Enable</button>
           </div>
         </div>

         <!-- Revert countdown -->
         <div id="ctrl-revert" class="ve-ctrl-revert" style="display:none">
           Auto-revert in: <span id="ctrl-revert-time">5:00</span>
         </div>

         <!-- Collapsible Override Log -->
         <div class="ve-override-log-section">
           <button class="ve-override-log-toggle" id="override-log-toggle">
             Override Log (<span id="override-log-count">0</span> events)
           </button>
           <div id="override-log" class="ve-override-log ve-override-log--collapsed">
             <div class="ve-text-dim">No events yet</div>
           </div>
         </div>
       </div>
       ```
       CRITICAL: All element IDs must be IDENTICAL to the old #page-power IDs. Do NOT rename any ID.

    3. **Restructure bottom grid into 2 explicit rows** — Replace the single `.ve-dashboard-bottom` div with two separate row divs:
       ```html
       <!-- Bottom Row 1: System Info (3 columns) -->
       <div class="ve-dashboard-row1">
         <!-- Move #inverter-status-panel, #status-panel, #health-panel here -->
       </div>

       <!-- Bottom Row 2: Analytics + Venus (2 columns) -->
       <div class="ve-dashboard-row2">
         <!-- Move #peak-stats-panel, #venus-os-panel here -->
       </div>
       ```

    4. **Delete entire #page-power section** — Remove everything from `<!-- Power Control Page -->` through the closing `</div>` of `#page-power` (lines 254-304).

    **style.css changes:**

    5. **Replace `.ve-dashboard-bottom`** with two explicit row classes:
       ```css
       .ve-dashboard-row1 {
         display: grid;
         grid-template-columns: repeat(3, 1fr);
         gap: 1rem;
         margin-bottom: 1rem;
       }

       .ve-dashboard-row2 {
         display: grid;
         grid-template-columns: repeat(2, 1fr);
         gap: 1rem;
       }
       ```
       Keep `.ve-dashboard-bottom` rules as-is for backwards compatibility (no harm, just unused). Or remove if clean.

    6. **Add compact power control inline styles:**
       ```css
       .ve-power-control-inline {
         margin-bottom: 1rem;
       }
       ```

    7. **Add collapsible override log styles:**
       ```css
       .ve-override-log-section {
         margin-top: 16px;
         padding-top: 12px;
         border-top: 1px solid var(--ve-border);
       }

       .ve-override-log-toggle {
         background: none;
         border: 1px solid var(--ve-border);
         border-radius: 6px;
         color: var(--ve-text-dim);
         padding: 6px 12px;
         font-size: 0.85em;
         cursor: pointer;
         transition: color 0.15s, border-color 0.15s;
         width: 100%;
         text-align: left;
       }

       .ve-override-log-toggle:hover {
         color: var(--ve-text);
         border-color: var(--ve-blue);
       }

       .ve-override-log--collapsed {
         display: none;
       }
       ```

    8. **Update responsive breakpoints** — In the mobile breakpoint (max-width: 768px), add:
       ```css
       .ve-dashboard-row1 {
         grid-template-columns: 1fr;
       }
       .ve-dashboard-row2 {
         grid-template-columns: 1fr;
       }
       ```

    9. **Add entrance animation delay for the new power control card.** It sits between gauge row and sparkline, so add:
       ```css
       .ve-card--entering:nth-child(9) { animation-delay: 480ms; }
       .ve-card--entering:nth-child(10) { animation-delay: 540ms; }
       ```

  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python3 -c "
import sys
html = open('src/venus_os_fronius_proxy/static/index.html').read()
css = open('src/venus_os_fronius_proxy/static/style.css').read()
errors = []
# No page-power nav item
if 'data-page=\"power\"' in html: errors.append('Power Control nav item still exists')
# No #page-power section
if 'id=\"page-power\"' in html: errors.append('#page-power section still exists')
# Power control IDs in dashboard
for eid in ['ctrl-slider', 'ctrl-apply', 'ctrl-toggle', 'ctrl-dot', 'ctrl-label', 'ctrl-limit', 'ctrl-source', 'ctrl-ts', 'ctrl-override-banner', 'ctrl-revert', 'override-log']:
    if f'id=\"{eid}\"' not in html: errors.append(f'Missing element ID: {eid}')
# Override log toggle
if 'override-log-toggle' not in html: errors.append('Missing override-log-toggle button')
if 'override-log-count' not in html: errors.append('Missing override-log-count span')
# 2-row grid
if 've-dashboard-row1' not in html: errors.append('Missing ve-dashboard-row1')
if 've-dashboard-row2' not in html: errors.append('Missing ve-dashboard-row2')
if 've-dashboard-row1' not in css: errors.append('Missing ve-dashboard-row1 CSS')
if 've-dashboard-row2' not in css: errors.append('Missing ve-dashboard-row2 CSS')
# Collapsible log styles
if 've-override-log--collapsed' not in css: errors.append('Missing collapsed log CSS')
if 've-override-log-toggle' not in css: errors.append('Missing log toggle CSS')
# Only 3 nav items
import re
nav_count = len(re.findall(r'class=\"nav-item\"', html))
if nav_count != 3: errors.append(f'Expected 3 nav items, found {nav_count}')
if errors:
    for e in errors: print(f'FAIL: {e}')
    sys.exit(1)
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep 'data-page="power"' index.html returns NO matches
    - grep 'id="page-power"' index.html returns NO matches
    - grep 'ctrl-slider' index.html matches inside #page-dashboard
    - grep 've-dashboard-row1' style.css returns match with grid-template-columns: repeat(3, 1fr)
    - grep 've-dashboard-row2' style.css returns match with grid-template-columns: repeat(2, 1fr)
    - grep 've-override-log--collapsed' style.css returns match with display: none
    - grep 'override-log-toggle' index.html returns match
    - Exactly 3 nav-item elements in index.html (Dashboard, Config, Registers)
  </acceptance_criteria>
  <done>Dashboard page contains all power control elements inline below gauge, bottom grid uses 2 explicit rows, Power Control page and nav item removed, override log is collapsible</done>
</task>

<task type="auto">
  <name>Task 2: Update app.js — override log toggle, nav cleanup, entrance animation fix</name>
  <files>src/venus_os_fronius_proxy/static/app.js</files>
  <read_first>
    - src/venus_os_fronius_proxy/static/app.js (full file — understand navigation, IIFEs, entrance animation logic)
    - src/venus_os_fronius_proxy/static/index.html (read the NEW version from Task 1 to see updated structure)
  </read_first>
  <action>
    **1. Add override log toggle functionality.** After the existing override log rendering code (after the updateOverrideLog function), add a new IIFE:
    ```javascript
    // --- Override Log Toggle (Collapsible) ---
    (function() {
        var toggleBtn = document.getElementById('override-log-toggle');
        var logContainer = document.getElementById('override-log');
        if (!toggleBtn || !logContainer) return;

        toggleBtn.addEventListener('click', function() {
            logContainer.classList.toggle('ve-override-log--collapsed');
        });
    })();
    ```

    **2. Update the `updateOverrideLog` function** to also update the event count badge. At the start of the function, after getting the container, add:
    ```javascript
    var countEl = document.getElementById('override-log-count');
    if (countEl) {
        countEl.textContent = (events && events.length) ? events.length : 0;
    }
    ```

    **3. Guard navigation against missing pages.** In the nav click handler (around line 22), wrap the classList.add('active') call with a null check:
    ```javascript
    var pageEl = document.getElementById('page-' + page);
    if (pageEl) pageEl.classList.add('active');
    ```
    Replace the existing `document.getElementById('page-' + page).classList.add('active');` line. This prevents errors if a stale nav reference tries to open a removed page.

    **4. Do NOT modify any power control IIFEs** — they bind by getElementById and will automatically find the moved elements since they remain in the DOM with identical IDs.

    **5. Verify entrance animation still works.** The entrance animation queries `#page-dashboard .ve-card` — the power control card is now inside #page-dashboard, so it will naturally be included in the entrance animation. The existing nth-child delays (up to 8th child with 420ms) should cover the additional card. If more than 8 .ve-card elements are direct children of containers within #page-dashboard, add delays up to the actual count.
  </action>
  <verify>
    <automated>cd "/Users/hulki/codex/venus os fronius proxy" && python3 -c "
import sys
js = open('src/venus_os_fronius_proxy/static/app.js').read()
errors = []
# Override log toggle
if 'override-log-toggle' not in js: errors.append('Missing override-log-toggle handler')
if 've-override-log--collapsed' not in js: errors.append('Missing collapsed class toggle in JS')
# Event count update
if 'override-log-count' not in js: errors.append('Missing override-log-count update')
# Navigation null guard
if 'page-power' in js and 'data-page=\"power\"' in js: errors.append('Still references page-power nav')
# The navigation should have a null guard
if 'if (pageEl)' not in js and 'pageEl &&' not in js and '?.classList' not in js:
    errors.append('Missing null guard on page navigation getElementById')
if errors:
    for e in errors: print(f'FAIL: {e}')
    sys.exit(1)
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep 'override-log-toggle' app.js returns match showing click event listener
    - grep 've-override-log--collapsed' app.js returns match showing classList.toggle
    - grep 'override-log-count' app.js returns match showing textContent update
    - Navigation getElementById has null guard (pageEl check before classList.add)
    - All existing ctrl-* IIFEs are UNCHANGED (slider preview, apply button, enable/disable toggle)
  </acceptance_criteria>
  <done>Override log toggle button works (expand/collapse), event count updates with each snapshot, navigation is null-safe after Power Control page removal, all power control JS bindings work unchanged</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of unified dashboard layout</name>
  <files>src/venus_os_fronius_proxy/static/index.html</files>
  <action>Human verifies the unified dashboard layout in the browser — all widgets visible, power control inline, override log collapsible, navigation cleaned up.</action>
  <what-built>Unified single-page dashboard with inline power control, collapsible override log, 2-row bottom grid, and removed Power Control navigation item</what-built>
  <how-to-verify>
    1. Open http://localhost:8080 in browser
    2. Verify Dashboard shows: Power Gauge > Power Control (slider, buttons) > Sparkline > Bottom Grid
    3. Verify Power Control slider, Apply, and Enable/Disable buttons are functional
    4. Verify Override Log is collapsed by default — click toggle button to expand
    5. Verify bottom grid has 2 rows: Row 1 (Inverter Status, Connection, Health), Row 2 (Performance, Venus OS)
    6. Verify sidebar has only 3 items: Dashboard, Config, Registers (no Power Control)
    7. Resize browser to mobile width — verify all sections stack vertically
    8. Verify entrance animations play on first WebSocket connect
  </how-to-verify>
  <verify>Human confirms layout is correct</verify>
  <done>User approves the unified dashboard layout</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. No `#page-power` section exists in index.html
2. No `data-page="power"` nav item exists in index.html
3. All ctrl-* element IDs exist inside `#page-dashboard`
4. Override log has toggle button and collapsed class
5. Bottom grid has explicit 2-row layout (ve-dashboard-row1, ve-dashboard-row2)
6. app.js has override log toggle handler and event count update
7. Navigation has null guard for removed pages
8. All power control JS functions (slider, apply, toggle, override log) work with moved elements
</verification>

<success_criteria>
- Power control appears inline below gauge on dashboard — no separate page needed
- All 8+ widgets visible on single dashboard page in compact grid
- Sidebar shows exactly 3 nav items (Dashboard, Config, Registers)
- Override log collapsed by default, expandable with event count badge
- All existing functionality preserved (slider, apply, toggle, revert countdown, override banner)
</success_criteria>

<output>
After completion, create `.planning/phases/12-unified-dashboard-layout/12-01-SUMMARY.md`
</output>
